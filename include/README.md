# 公共头文件

`include/` 保存跨组件构建使用的公共头文件。它包含原有兼容头文件，以及从仓库根目录整体迁入的 `X11/` 和 `gl/` 头文件树。

这些目录可能混合协议头、库 API 头、平台兼容副本和生成配置；不能把整个 `X11/` 或 `gl/` 目录归为单一上游提交。具体来源、版本证据和未知边界见[依赖来源清单](../docs/dependencies/SOURCES.md)。旧路径与当前位置的精确映射见[源码路径映射](../docs/history/2026-09-10-source-path-mapping.json)。
