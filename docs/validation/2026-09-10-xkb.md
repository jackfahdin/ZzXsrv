# XKB 修复候选验证

状态：修复、完整构建、158 项测试、独立运行验证和审查已通过；尚待本组实际使用确认，**未合入 master，未 push**。

## 范围与源码

基线为 `1a5a0f7e3988eabd36b31d15eb7744be0a14d82f`，临时分支为 `codex/xkb-20260910`。本地生产代码提交及固定构建来源为 `b9081dd495558c1f0f8ef911179015ae109b4256`，见 [来源清单 JSON](../dependencies/SOURCES.json) 的 17 条 XKB 记录；独立目录为 `D:/File/Program/GitHub/zzxsrv-xkb-20260910`。

本次只处理[原审计](2026-09-09-xserver-xkb-audit.md)已确认的 8 项问题：CVE-2025-62230、CVE-2025-62231、CVE-2026-33999、CVE-2026-34000、CVE-2026-34002、CVE-2026-34003、CVE-2026-50258、CVE-2026-50259。前置和配套修复一并纳入，共 17 个上游提交；不把配套提交重复计为额外 CVE。

| 范围 | 变更 |
| --- | --- |
| 资源生命周期 | 删除 XKB interest 时同步删除资源表项，并公开模块内部的资源类型；通过 skip 类型避免重复触发删除回调 |
| compat 映射 | 限制解释项总数，维护复用缓冲区的有效数量，保留本地已有的跳过项及尾部整理逻辑 |
| 键盘映射请求 | 检查类型数、层数、描述符、map/preserve、符号及动作区，以及 modifier/behavior/explicit/vmod 区；保留合法空符号和零动作 |
| 几何数据 | 检查 alias 双名称、颜色和轮廓索引、overlay 行索引与分配结果；修正 section doodad 容量比较 |
| 辅助函数 | 修正 CFile 虚拟修饰键文字前缀长度，realloc 失败时保留原字符串 |

没有整体替换 X Server，也没有修改键盘操作策略、剪贴板或 GUI-001。其余 R3 项目及 R4–R9 仍按[状态清单](../maintenance/PLAN_STATUS.md)推进。

## 来源核验

17 个提交的实际补丁从固定 SHA 的 GitHub 镜像 API 取得，完整下载地址、原作者、稳定分支 SHA、下载时间、响应与提取补丁 SHA-256 均记录在 [SOURCES.md](../dependencies/SOURCES.md) 和 JSON。官方 GitLab 链接用于上游标识，不冒充实际下载入口。

官方下载包 `xorg-server-21.1.24.tar.xz` 的 SHA-256 再次核对为 `1a4eb36ca65cc3b1b936566d677a9786e13c11cd5806e951ac55f3f5ce3984af`。17 个原始补丁按前置顺序在原基线上独立重放，结果与最终 6 个生产文件完整一致；127 行非空新增内容与稳定发布源码核对。独立审查进一步确认 17 个涉及的函数体忽略空白后与发布源码一致。

路径仅增加 `src/xorg-server/` 前缀，无额外生产代码改写。资源类型声明遵循上游 master 和当前本地的 `xkb/xkbsrv_priv.h`；稳定版较旧结构使用 `include/xkbsrv.h`，并未为追求文件一致而改变本地头文件边界。

## 自动测试与审查

原生测试直接包含项目中的 C 翻译单元，使用 MSVC x64 AddressSanitizer；通过受控本地缓冲区、分配失败和资源表夹具验证行为，不向运行中的服务器发送异常请求。未执行的服务入口使用立即中止的链接别名，实际执行路径使用具备类型和断言的夹具。资源表的 skip 行为另与 DIX 实现核对；测试不是完整 DIX 集成测试。

| 专项 | 修复前 | 修复后 |
| --- | --- | --- |
| 请求检查 | 45 项中 26 项失败，19 项正常路径通过 | 原 45 项全部通过 |
| interest 生命周期 | 6 项中 4 项失败 | 6 项通过 |
| 辅助函数 | 9 项中 4 项失败 | 9 项通过 |
| 审查补充 | 原有代码语义的额外回归覆盖 | 新增已有尾部的 compat 跳过项整理，46 项请求测试通过 |

原始失败中包含类型表越界、section doodad 和 CFile 缓冲区错误的 ASan 诊断。测试中的异常仅存在于独立子进程；这些证据不构成当前运行配置下的网络可利用性结论。请求帮助函数新增 `ClientPtr` 后，测试对应 6 处调用同步增加 client 参数，原断言不变。

独立审查结论为规格通过、质量批准，没有阻塞问题；一项非阻塞建议已补入现有尾部整理测试并通过定向复审，来源记录亦已独立复核。编译前完整测试运行 157 项，154 项通过、3 项因新工作区尚无 mhmake/字体工具而跳过。构建后全套测试 158 项通过、0 失败、0 跳过，耗时 24.954 秒；已包含审查追加用例，且使用本组新产物。

审查追加测试提交为 `65a8bf7cef77702b2ea91837fde347ccf61acf19`，不改变生产代码或构建来源。

## 构建与运行验证

在候选根目录执行：

```powershell
.\scripts\build\buildall.ps1 -Jobs 30
```

完整 All 构建一次通过，开始 `2026-09-10T23:45:40.0422033+08:00`，结束 `2026-09-10T23:56:06.2827989+08:00`，退出码 0。构建前后 HEAD 均为 `b9081dd495558c1f0f8ef911179015ae109b4256`；新增测试及文档不参与生产构建，构建结束后才提交。运行目录为 `D:/File/Program/GitHub/zzxsrv-xkb-20260910/dist/x64/Release`。

| 检查 | 结果 |
| --- | --- |
| 全套测试（本地工具及真实运行均启用） | 158 项通过，0 失败、0 跳过，24.954 秒 |
| 独立运行检查 | 所有步骤通过，耗时 1.318 秒；包含依赖、版本、认证启动、根窗口查询和清理 |
| 便携文件及压缩字体 | 5,129 个文件，与前组文件集合一致；4,297 个压缩字体解压及 PCF 文件头检查通过 |
| 既有运行文件 | 350 个 EXE/DLL 的 SHA-256 全部保持不变 |

本组开始前后对比的 350 个既有 EXE/DLL 均未变化。运行验证使用临时认证和独立显示号，不改系统防火墙，不接触用户剪贴板或正在运行的应用。

本机详细证据在候选目录 `.local-validation/xkb-20260910/`：原始补丁及下载回执、来源重放核验、修复前后测试、独立审查、构建日志、运行验证、便携文件清单及既有文件哈希比较。

## 待实际使用确认

本候选需要单独确认启动、退出、再次启动，窗口和菜单操作，普通键盘输入及常用快捷键；有 Linux 图形环境时再验证相应应用的字符输入和组合键。未具备环境的场景明确保留为未验证。

此前输入或 RENDER/RECORD 的“使用正常”不能替代本组验收。跨屏菜单 GUI-001 按维护者要求继续暂缓，不作为已修复项目。实际使用确认之前保留候选分支，master 维持已验收状态。

构建证据为 `build-result.json` / `build.log` / `environment.json`；完整测试为 `tests-final.log`，独立运行是 `runtime/result.json`，便携与旧文件核对是 `artifact-verification.json` / `old-runtime-comparison.json`。这些检查不替代实际应用、快捷键、中文输入、剪贴板、OpenGL 或多显示器场景的人工确认。
