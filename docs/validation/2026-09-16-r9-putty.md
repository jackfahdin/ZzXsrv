# R9.6 PuTTY (Plink) 0.85 升级

日期：2026-09-16。基线 `821ce2da1dd239a93e9f5d1ff05de37b4e5adf64`；分支 `codex/r9-putty-20260916`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r9-putty-20260916`。

状态：候选待验收。`tools/plink` 从继承的 0.83+24（`50e360fc`）升级为官方 0.85 发布归档，完整 x64 Release All 构建通过，452 项回归通过（0 跳过，含 3 项新增 plink 检查），plink.exe 红绿冒烟通过，候选独立运行检查 PASS，交付快照与构建输出一致。真实 SSH 转发场景未实测；分支、工作区与运行目录保留，未推送、未合入 master。

## 来源与校验

来源采用官方上游 0.85，未采用 larryli/PuTTY 中文 fork（R9.5 已评估并记录）。发布接口 <https://the.earth.li/~sgtatham/putty/0.85/> 提供 `putty-0.85.tar.gz` 及 sha256sums、GPG 签名。

- 归档 SHA-256：`13fd4db2936d03b73812a7bcc2a658e4dd29cc776a56c3670a7fc6f1a0ee8af8`，与官方 clearsigned `sha256sums` 一致（`sha256sum -c` OK）。
- GPG：`sha256sums.gpg` 与 `putty-0.85.tar.gz.gpg` 均由 PuTTY 2026 发布密钥签署（指纹 `1A07 DAC0 F220 CF12 248E A719 5C67 D0F8 8BC3 E5B5`，与官方 keys 页一致，公钥取自官方 keys/release-2026.asc），`gpg --verify` 均为 Good signature。
- 与 git tag 0.85（`a3cce3a48d4b…`）交叉核对：tag 688 文件与归档 686 个逐字节一致；2 个差异为发布戳（`version.h` 盖 Release 0.85、`cmake/gitcommit.cmake` 写入 tag commit），归档另有 36 个生成文档（doc/html、man、chm 等）不在导入范围。

## 导入与适配处置

以 GPG 验证的发布归档（含发布戳 version.h）导入 688 个文件；排除 36 个生成文档，与此前导入范围一致。逐文件比对（`tools/plink/source-audit.json`）：683 个逐字节相同、0 个仅换行差异、5 个适配改动、9 个仅本地文件、0 个仅上游存在。

6 个继承适配逐条复核：保留 5 个——`.gitignore` 清空、`defs.h` NDEBUG `#error` 保护块移除、`settings.c` qsort/sfree `(void*)` 强转、`windows/gss.c` `SECURITY_WIN32` 加 `#ifndef`、`windows/plink.c` 补 `#include <fcntl.h>`；丢弃 1 个——`ssh/x11fwd.c` 的 `socketdatalen` 初始化，0.85 上游已自行修复（ssh/x11fwd.c:594 `socketdatalen = 0; /* placate compiler warning */`）。适配汇总为 LF 补丁 `tools/plink/patches/local-adaptations.patch`，已对 pristine 0.85 内容验证 `git apply -p1` 后 5 个文件逐字节复现。

7 个本地文件（6 个 mhmake makefile、`version.c` 桩）保留。makefile 清单核对：0.83+24 → 0.85 上游新增 13 个 .c、无删除改名；其中 4 个属于 plink 构建路径并已同步——`proxy/newmain.c`（`new_main_connection` 被 ssh/ssh.c 与 otherbackends 调用，加入根 makefile）、`utils/format_connection_setup_command.c`（被 proxy/telnet.c、newmain.c 调用，加入 utils/makefile）、`windows/utils/blink_time.c` 与 `windows/utils/subprocess_waiter.c`（加入 windows/makefile UTILS_CSRCS）；`ssh/nocompress.c` 与既有 zlib.c 互斥不加，test/unix 文件不参与本构建。核对脚本确认 6 个 makefile 全部源引用可解析（missing_files 为空）。

## 候选验证

- 完整构建：`buildall.ps1 -Stage All -Jobs 16`，显式 PerlPath/NasmPath，子进程 PATH 剔除 Git Bash 目录；2026-09-16 23:23:42–23:35:48（+0800），退出码 0。环境报告 `environment-all.json` 记录构建启动时 HEAD `821ce2da1d`；导入在构建期间提交为 `e0cad4dbca888c12b88629e76b74726d39642f8d`，提交未改变工作区内容，构建产物对应 `e0cad4dbca` 的树，`build-all-result.json` 以此记录。
- 红绿冒烟：旧版（主仓库 dist，0.83+24）`-V` 输出 "Unidentified build"；新构建 `dist/x64/Release/plink.exe` `-V` 输出 "plink: Release 0.85"，退出码 0；无效参数退出码 1 与旧版一致。dist 的 plink.exe 与 `tools/plink/obj64/release/plink.exe` SHA-256 相同（`d098b416…c547`）。
- 全量回归：vcvarsall amd64 环境、`VCXSRV_TEST_LOCAL_TOOLS=1`、`VCXSRV_TEST_RUNTIME=1`、`VCXSRV_RUNTIME_DIR` 指向本工作区 dist，`unittest discover` 452 项通过、0 跳过，48.234 秒（`tests-final.log`，UTF-16；UTF-8 副本 `tests-final-utf8.txt`）。基线 449 项，新增 3 项 plink 真实检查（`tools/tests/test_plink.py`）：plink.exe 为 x64 PE、`-V` 报告 Release 0.85、无效选项非零退出。
- 独立运行检查：`tools/verify_runtime.py` PASS，1.139 秒（`runtime/result.json`）。
- 交付快照：`dist/x64/Release` 5152 个文件、33 个 PE，全量 SHA-256 见 `portable-files.json`；plink.exe 与构建输出一致。

## 证据与边界

本地证据位于 `.local-validation/r9-putty-20260916/`（不入库）：发布归档与校验/签名文件、`gpg-verify.txt`、`crosscheck-tag.json`、`import-result.json`、`adaptation-dispositions.md`、`makefile-check.json`、构建与测试日志、`runtime/result.json`、`portable-files.json` 等。

验证目标为 x64 Release。plink 的实际 SSH 连接与 X11 转发场景（XLaunch 调用 plink.exe 建隧道）未实测；Win32、Debug、上游 CMake 构建与上游测试集不在通过范围。旧版 plink 行为对照仅覆盖 `-V` 与无效参数处理。传统字体、OpenGL、双向剪贴板等此前未确认场景继续保留。

## 下一步

候选交付维护者验收；R9 剩余组件的固定来源补证尚未实施。验收后由主线统一登记 PLAN_STATUS 与候选清单并合入。
