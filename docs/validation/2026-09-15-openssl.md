# R7 OpenSSL 3.5.8 LTS 升级

日期：2026-09-15。基线：`1d8a0133e59fd9a5f858e17147213dc4f678602c`。
分支：`codex/openssl-20260915`。独立目录：`D:/File/Program/GitHub/zzxsrv-openssl-20260915`。

2026-09-15 取得维护者“R7 正常，合入”的反馈后，已快进合入 master，未推送。完整 x64 Release 构建、410 项自动回归和独立运行检查通过；构建源码为 `e41de0979d1c842157656c704529a3bf009c6636`。后续仅修正未参与产品构建的示例文件名大小写及补充文档，合入验证见下文。

## 范围与来源

[官方版本列表](https://openssl-library.org/source/)列出 3.5.8 于 2026-08-25 发布；[生命周期](https://openssl-library.org/roadmap/)列出 3.5 为 LTS，支持至 2030-04-08。[3.5 系列变更记录](https://openssl-library.org/news/openssl-3.5-notes/)说明版本变化；不把所有 TLS/QUIC/CMS 变化视作本产品实际调用路径。

官方归档 SHA-256 为 `a8f84a39918ec6415ce765d9b429d313ba97b8143169c172e734b9514464f5b2`，与官方 SHA-256 文件及 GitHub release asset digest 一致，未本地独立验证 PGP 签名。归档 5767 个普通文件逐字节核对，无链接或 Windows 大小写冲突。tag 对象为 `090eec6d3628aa0520bdf2cf97b063fafc34e7be`，解引用提交为 `f4dc4d58b48d346a8270183f89acf826d459b0ca`，通过公开 `git ls-remote` 核对。

`third_party/openssl` 保留官方全部文件；本地附加来源说明和文件摘要清单。根 Git 属性保持原始字节，根忽略规则覆盖四个 Windows 构建目录。保留原生 Perl Configure、VC-WIN64A/VC-WIN32 目标及串行 OpenSSL make 阶段，不改 DLL 命名或输出映射。四份 NSIS 清单及历史 mkzip 增加许可证和来源说明；未执行 NSIS 安装包构建。

Windows X Server 的直接消费者为 `os/xsha1.c`，使用 SHA1_Init/Update/Final 供字形哈希；现有 libssl 导入库虽列入链接输入，源码未发现 TLS API 消费。本次保留该接口和行为，最终以产品导入表及实际运行结果复核。依赖来源目录仍为 198 项组件、39 条上游补丁、4 条本地兼容记录；更新 OpenSSL 和内嵌 Text::Template 的归档证据，旧 3.4.1 继承记录保留。

## 开发验证

新工作区构建前运行 404 项测试，46 项执行通过、358 项因无构建产物跳过，8.523 秒。这不是完整产品验证。

新增测试链接真实生产 `xsha1.obj`、OpenSSL 导入库并将交付 DLL 放在测试程序旁。旧 R6 产物对照：5 个 SHA-1 用例通过，包括空输入、短输入、多块输入、百万字节分块更新及零长度更新；版本检查按预期失败，读到 3.4.1 而非目标 3.5.8（6 项，0.143 秒）。首次测试工具编译因 MSVC 对 fopen 的弃用警告失败，按本地 C 测试惯例声明 `_CRT_SECURE_NO_WARNINGS` 后取得上述结果。

构建环境预检已通过，仍使用 VS2022/MSVC、Windows SDK、Strawberry Perl、NASM、jom 及现有 Python/CMake 工具。首次调用 Windows PowerShell 未指定脚本执行参数，被宿主策略拒绝；按仓库既有方式使用进程级 `-ExecutionPolicy Bypass` 后预检通过，没有修改系统策略。

证据保存在本工作区忽略目录 `.local-validation/openssl-20260915/`，包括官方发布记录、校验文件、完整归档、源码摘要、基线、旧版对照与构建环境报告。旧组件在其中 `inherited-openssl/`；R6 运行目录摘要保存在 `r6-files-before.json`，交付前复核。

## 来源与接口审查

独立审查将全部继承 Git blob 与官方 3.4.1 Git tree 比较：共同路径中仅 `.gitignore` 和 `Configure` 不同，全部 `Configurations/*`（包含 Windows 模板及目标）一致。旧 Configure 的精确祖先尚未证实，不能称为完全原样的旧官方版本；没有识别出需保留的专门 Windows 适配，本次替换为官方 3.5.8 Configure。旧本地附加的两个忽略文件及构建目录忽略规则由仓库根规则接替。

首次提交前的 Git 路径检查发现继承的 `demos/sslecho/makefile` 仍以小写记录，而官方文件名为 `Makefile`。验证脚本报告失败，但外层 PowerShell 未传递该检查退出码，提交命令仍继续执行；随后明确修正 Git 文件名大小写，在 `92985c583bac165781712aa93a8174c7eafb3530` 复核全部路径、文件内容和 Git blob 均通过。此示例文件不参与 Windows 产品构建。后续检查显式传递子进程退出码。

独立审查再次确认：5767 个官方文件、5767 条摘要、5769 个跟踪文件，没有缺失路径或 blob 摘要差异，仅来源说明和摘要清单为本地附加文件。审查还核对了真实字形调用及测试对象，补上零长度更新用例；未发现阻塞性问题。审查证据保存在 `review/` 子目录，不能代替下述构建和运行验证。

## 验证边界

本轮完整产品仅构建 x64 Release；未构建完整 Win32/Debug 产品，未执行 NSIS 安装包。上游仅运行 SHA 专项，不等同于完整 OpenSSL 上游测试集或逐项安全公告复现；FIPS 配置准备项因非 FIPS 构建而跳过，不能表述为 FIPS 验证。

OpenGL、传统字体、双向剪贴板及其他未确认人工场景继续保留，不因依赖升级自动标为通过。本版本已取得使用正常的反馈并整合；未提供逐项测试明细，未确认场景不据此补为通过。

## 完整构建与最终验证

使用 Windows PowerShell 5.1、VS2022/MSVC 19.44.35227、SDK 10.0.26100.0、Strawberry Perl 5.42.2.1、NASM、jom 1.1.7.0、Python 3.14.2 及既有 VS CMake。执行 `scripts/build/buildall.ps1 -Stage All -Jobs 16`，2026-09-15 14:15:03–14:26:52 完成依赖、服务器、消费者和 Portable，退出 0。OpenSSL 子阶段仍为串行 `/J1`。本轮没有构建失败后的恢复步骤。

构建开始时记录源码 `e41de0979d1c842157656c704529a3bf009c6636`；期间完成的 `92985c583bac165781712aa93a8174c7eafb3530` 只修正上节所述示例文件名，没有修改编译输入内容。环境、命令和退出结果分别保存在 `environment-all.json`、`build-all.log`、`build-all-outcome.json`。

| 验证 | 结果 |
| --- | --- |
| 完整自动回归 | 410 项通过、0 跳过，45.280 秒；启用本地工具与实际运行检查，使用 R7 本工作区的库、对象和交付目录 |
| R7 OpenSSL 消费者 | 6 项全部通过，包含在上述完整 suite；真实生产 xsha1.obj 与交付 DLL 的版本、空输入、短输入、多块输入、百万字节更新、零长度更新 |
| 旧对象与新 DLL | R6 生产对象/导入库配 R7 交付 DLL，6 项通过、0 跳过，0.169 秒；不混作 R7 全产品结果。打包前的新库对照另为 6 项、0.164 秒 |
| 上游 SHA 专项 | `nmake /nologo test TESTS=test_sha`，15-test_sha.t 通过，Files=1、Tests=1、Result: PASS；非 FIPS 准备项跳过。Perl 对继承的 C.UTF-8 locale 警告后回退系统 locale，测试退出 0 |
| 产品导入表 | vcxsrv.exe 从 libcrypto-3-x64.dll 导入 SHA1_Init/Update/Final，没有新增 libssl 依赖 |
| 独立运行检查 | PASS；33 个 EXE/DLL 的架构和依赖、版本、带认证启动、根窗口查询及清理通过，见 runtime/result.json |
| 交付目录 | 5136 个实际文件，33 个 EXE/DLL；相较 R6 只增加 licenses/openssl/LICENSE.txt 和 README.vcxsrv.md 两个路径，没有新增运行 DLL。打包器的 5139 是含重复项的拷贝条目数 |
| R6 目录保护 | 5134 个原文件的集合和 SHA-256 全部不变 |
| 来源与跟踪路径 | 5767 个上游文件内容及 Git blob 摘要一致，官方路径大小写一致；仅两个本地附加文件 |

上游 SHA 目标执行中还链接了未随产品打包的 libssl、引擎和 legacy provider；检查后生产 libcrypto 与交付 DLL 的 SHA-256 仍完全相同：`1498ee9a73e14de9b50fee4b722ddd9550b3c33f55f8c51a1f89205ad41e83bd`。不将这些额外构建工具写成新增运行依赖。

最终证据还包括 `tests-final.log`、`old-object-new-dll.log`、`upstream-sha.log`、`vcxsrv-imports.txt`、`runtime/result.json`、`portable-files.json` 和 `verification.json`。`verify-candidate.py` 复核来源、构建结果、测试结果、实际文件集合、R6 保护、主线未变及文档链接。

## 运行入口与实际验收

运行入口：`D:/File/Program/GitHub/zzxsrv-openssl-20260915/dist/x64/Release/xlaunch.exe`。交付时建议验证启动、gitk 操作、中文显示和关闭后重启；现已取得“R7 正常，合入”的反馈，记录为本版本实际使用正常，不补造逐项明细。

R7、R6 及更早运行目录继续保留。主目录旧 dist 没有重建，继续使用上述 R7 入口；未推送。

## 实际反馈与主线整合

2026-09-15，维护者反馈“R7 正常，合入”。master 从 `1d8a0133e59fd9a5f858e17147213dc4f678602c` 快进至已验收候选 `d849e54af990c5538a606fbcacbec0ee530c3ec6`，随后仅补充整合文档。

合入前后分别在已构建的 R7 工作区运行完整回归，均为 410 项通过、0 跳过，耗时分别为 45.321 秒和 43.452 秒。合入后测试时该工作区与 master 位于相同候选提交，使用 R7 实际产物；没有将主目录旧 dist 当作 R7 产物。

复核 R7 交付清单的 5136 个原文件，集合及 SHA-256 与交付时一致。后续只更新文档，生产代码、测试及交付产物未变；依赖记录仍为 198 项组件、39 条上游补丁、4 条本地兼容记录。

整合证据位于主工作区 `D:/File/Program/GitHub/vcxsrv/.local-validation/openssl-integration-20260915/`，包含 `tests-before-merge.log`、`tests-after-merge.log` 和 `verification.json`。候选构建、来源及验证材料保留原样；分支和工作区登记继续保留，未 push。
