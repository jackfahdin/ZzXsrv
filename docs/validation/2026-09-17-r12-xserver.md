# R12 xserver 上游 21.1 分支安全补丁批次整合

日期：2026-09-17。基线 `ddb2b72529`；分支 `codex/r12-xserver-20260917`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r12-xserver-20260917`。

状态：候选待验收。本轮按 [R11 适用性评估](2026-09-17-r11-xserver-applicability.md) 的 A 档，从 server-21.1-branch cherry-pick 19 个安全/崩溃修复到 `src/xorg-server`（render、dix、Xi、Xext、glx），每个补丁一个本地提交并保留上游作者署名。

## 应用结果

19 个补丁中 **17 个干净 cherry-pick**（上下文逐行一致或仅行号偏移），**2 个手工适配**：

- `b171669dc0c9`（Xi: wOtherInputMasks NULL 判空，5 个 hunk）：第 4 个 hunk（DeviceEventSuppressForWindow）因本地 fork 将 `RT_NONE` 改名为 `X11_RESTYPE_NONE` 手工套用，语义与上游一致，其余 4 个 hunk 干净。
- `976ef43f95a3`（Xext/sync: malloc 失败返回已释放指针）：上下文因同一改名手工套用，`return pCounter;` → `return NULL;` 的语义修改与上游逐字一致。

sync 三连按上游依赖顺序应用（`7097560cfb23` → `9bad510a45ca` → `976ef43f95a3`）。19 个补丁的完整清单、本地提交对应与适配备注已登记到 `docs/dependencies/SOURCES.md`（R12 批次表）与 `SOURCES.json` 的 `applied_upstream_patches`（39 → 58 条）。

## 构建与回归

- 完整 x64 Release All 构建通过（`buildall.ps1 -Stage All -Jobs 16`），便携运行目录 `dist/x64/Release` 共 5155 个交付文件。
- 452 项回归通过、0 跳过（49.700 秒，含运行时检查），直接针对本候选的新构建运行目录运行。构建/回归源码 `2fe3f05341be968ede39821bbc6f8abb93dce83f`。
- 候选运行目录：`D:/File/Program/GitHub/zzxsrv-r12-xserver-20260917/dist/x64/Release`（入口 `xlaunch.exe`）。

## 证据与范围

证据目录 `.local-validation/r12-xserver-20260917/`（git 忽略、不入库）：19 个上游补丁文件（patches/）、逐个 apply 记录（apply-*.err，干净项为空文件）、452 项回归日志（candidate-tests.log）。

本轮范围：仅 R11 A 档 19 个补丁；B/C 档未导入（xkb 成对修复、rootless 三个等留待后续评估）。修复本身由上游稳定分支发布，触发这些漏洞需构造特定 X 请求；本轮未逐项构造攻击输入复现（修复的正确性由上游发布与本地代码审查共同支撑），日常 GUI 场景待维护者实测。

## 下一步

候选交付维护者验收。建议实测：启动、常用操作（gitk 等）、多窗口、中文显示、剪贴板、重启——与既往各轮相同；SSH 转发等既往未测边界不受影响。验收后快进合入 master 并跑合入后回归。
