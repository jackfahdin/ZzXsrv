# Windows x64 Release 本地维护基线

> 目录迁移说明（2026-09-10）：文档链接已更新，正文中的旧命令与文件路径仍表示当时状态；当前入口见[文档导航](../README.md)。

> 2026-09-10 历史说明：本文保留当时的方案、提交号与验证结果。旧分支、标签和工作区登记已清理，源码及运行目录保留为普通目录；旧命令不应直接照搬执行。现行规则见[维护规则](../maintenance/UPSTREAM.md)，对应关系与恢复材料见[整理记录](2026-09-10-repository-reorganization.md)。旧依赖快照清单见[历史 JSON](../history/2026-09-09-dependency-snapshot.json)；当前不再维护上游源码分支。

结果：**PASS**。2026-09-09 在当前 Windows 主机从固定提交的新 worktree 完成 All 构建、43 项测试与经认证的最小 X 服务验证。未使用 WSL/Cygwin，未下载或安装工具，未执行远程操作。

## 来源与证据位置

- 被编译、测试的源码提交：`a4adc3dc3f2158c2308133cecee19a71cf62bcc8`。
- 本地注释标签：`local-baseline-20260909-a4adc3dc`，指向上述源码提交。
- 本报告在随后独立的文档提交中加入；标签所指提交不包含本报告，不代表文档提交重新编译过。
- 运行编号：`20260909-121720`；开始于 `2026-09-09 12:17:20 +08:00`，结束于 `12:22:04 +08:00`。
- 干净源码目录：`D:\File\Program\GitHub\vcxsrv-baseline-20260909-121720`。
- 本次运行目录：`D:\File\Program\GitHub\vcxsrv-baseline-20260909-121720\dist\x64\Release`。
- 原始证据目录：`D:\File\Program\GitHub\vcxsrv\.worktrees\maintenance-baseline\.local-validation\20260909-121720`。

证据目录保留 `summary.json`、`checkout.log`、`initial-status.txt`、`environment.json`、`build.log`、`tests.log`、`verify.log`、`runtime/result.json`、`runtime/dependencies.json`、各子进程日志、`prebuilt-inventory.json`、`artifacts.json` 和 `tracked-changes.txt`。完整机器日志与二进制未加入 Git。

主工作目录 `D:\File\Program\GitHub\vcxsrv` 中原有的 `dist\x64\Release` 未覆盖。构建开始前 D 盘剩余空间约 756 GiB。初始检出状态为空，构建后受跟踪源码没有变化。独立 worktree 与运行目录保留供复查。

## 实际工具环境

完整选中路径见本次 `environment.json`；下面的版本来自该报告。

| 工具或环境 | 本次记录 |
| --- | --- |
| Windows | Microsoft Windows NT 10.0.26200.0 |
| 实际构建 PowerShell | Windows PowerShell 5.1.26100.9444 |
| VS / MSVC 工具目录 | VS2022 Community；14.44.35207 |
| cl / dumpbin 文件版本 | 14.44.35227.0 |
| MSBuild | 17.14.40+3e744208875e56e4bf0bc22c40a1c431fb150987 |
| Windows SDK | 10.0.26100.0；`D:\Windows Kits\10\` |
| Python | 3.14.2；`D:\SoftWare\Python3\python.exe` |
| Python 包 | lxml 6.1.3、mako 1.4.1、PyYAML 6.0.3 |
| Perl 文件版本 | 5.42.2.1 |
| Gperf 文件版本 | 3.0.1.1765 |
| JOM 文件版本 | 1.1.7.0 |
| Flex / Bison | `D:\SoftWare\win_flex_bison-2.5.25\`；报告版本为 null |
| NASM | `D:\SoftWare\Strawberry\c\bin\nasm.exe`；报告版本为 null |

null 表示没有取得可靠文件版本，不表示工具缺失。文件版本与工具命令行显示的版本可能采用不同编号。任务 1 另在 Windows PowerShell 5.1 与 PowerShell 7.6.5 验证过 CheckOnly 环境报告；本次完整编译由 Windows PowerShell 5.1 执行，外层证据收集使用 PowerShell 7。

## 预编译依赖边界

Git 跟踪的 DLL/LIB 共 10 个，合计 14,050,089 字节，全部位于 `libxml2`。清单记录了每个文件的相对路径、大小和 SHA-256。

x64 流程使用 `libxml2/lib64/libxml2-2.lib`，并从 `libxml2/bin64` 整理 `libxml2-2.dll`、`libiconv-2.dll`、`libwinpthread-1.dll`、`libgcc_s_sjlj-1.dll`。对应 `bin` 与 `lib` 下的 32 位文件也在仓库清单中，但不是本次 x64 运行依赖。运行目录另外携带现有 VS 安装中的 MSVC 运行库。

因此，本结果证明新源码目录可完成既有构建流程，不表示所有第三方依赖都从源码重建，也不要求重复构建的二进制逐字节相同。`artifacts.json` 保存本次 35 个 EXE/DLL 的路径、大小和 SHA-256，用于识别本次产物。

## 执行命令与结果

下面记录本次核心命令。`$cleanRoot` 和 `$evidenceRoot` 对应上面的实际路径；复跑应创建新的检出和证据目录。命令在独立 PowerShell 进程执行，工具选择取自本次报告。

```powershell
$testedCommit = 'a4adc3dc3f2158c2308133cecee19a71cf62bcc8'
git worktree add --detach $cleanRoot $testedCommit
Set-Location -LiteralPath $cleanRoot
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\buildall.ps1 -Configuration Release -Architecture x64 -Jobs 8 -EnvironmentReport (Join-Path $evidenceRoot 'environment.json')
$environment = Get-Content (Join-Path $evidenceRoot 'environment.json') -Raw | ConvertFrom-Json
$selectedPython = $environment.tools.python.path
$env:VCXSRV_TEST_LOCAL_TOOLS = '1'
$env:VCXSRV_TEST_RUNTIME = '1'
$env:VCXSRV_RUNTIME_DIR = (Resolve-Path dist/x64/Release).Path
$env:VCXSRV_DUMPBIN = $environment.tools.dumpbin.path
$env:VCXSRV_SOURCE_COMMIT = $testedCommit
& $selectedPython -B -m unittest discover -s tools/tests -v
& $selectedPython -B tools/verify_runtime.py --runtime dist/x64/Release --dumpbin $environment.tools.dumpbin.path --source-commit $testedCommit --output (Join-Path $evidenceRoot 'runtime') --display 97 --timeout 30
git status --porcelain --untracked-files=no
```

外层执行器逐步保存输出并检查退出码；失败时终止后续步骤，保留失败记录。

| 步骤 | 状态 | 退出码或证据 |
| --- | --- | --- |
| 新建固定提交的干净检出 | PASS | 0；初始状态为空 |
| All 编译及 Portable 整理 | PASS | 0；`build.log` |
| 完整脚本与集成测试 | PASS | 0；43 项，0 失败、0 跳过，用时 30.168 秒 |
| PE 架构及直接/延迟导入 | PASS | 0；35 个 EXE/DLL，运行目录与原生 System32 解析，API-set 记为系统契约 |
| 版本、认证、根窗口查询 | PASS | 验证器退出 0；根窗口 1920×1080 |
| 自有子进程和认证文件清理 | PASS | 子进程退出，临时认证文件移除 |
| 受跟踪源码状态 | PASS | 无变化；`tracked-changes.txt` 为空 |

真实运行集成用例验证了原运行目录成功、临时副本删除 `libX11.dll` 后失败、恢复文件后的含空格路径成功。mhmake 已在干净构建中生成，因此其集成用例实际执行，没有跳过。

认证验证采用临时随机 Xauthority、显示号 97、有限超时及最小 PATH（运行目录和 System32），没有使用 `-ac` 或修改全局防火墙。服务器在查询时仍运行，因此 `startup.exit_code` 为 null；成功由客户端退出 0 和正的根窗口尺寸共同确认，随后执行进程清理。1920×1080 只记录本机结果，不是固定验收尺寸。

## 验收对照与失败记录

| 规格 | 结果与依据 |
| --- | --- |
| B1 环境报告 | PASS；实际选中工具、目标、主机和 Python 包已记录 |
| B2 运行依赖 | PASS；35 个 PE，含缺 DLL、错误架构等失败路径测试 |
| B3 自动启动 | PASS；最小 PATH 下版本查询、临时认证连接、正尺寸及清理 |
| B4 干净构建 | PASS；固定提交的新目录、All 成功、43 项测试及运行验收 |
| B5 证据与失败定位 | PASS；分步 JSON、退出码和日志，认证内容隐藏，失败不伪装成功 |
| B6 本地基线 | PASS；本报告关联被测提交，本地注释标签仅指向被测源码 |

首次辅助执行尝试 `20260909-121632` 失败：外层 Windows PowerShell 5.1 在 `ErrorActionPreference=Stop` 下将 Git 正常 stderr 进度作为异常处理。Git 检出完成，但尚未开始编译。保留其 worktree 和 `summary.json`，将外层执行器改为 PowerShell 7 后使用新编号重新运行；本次实际构建入口仍为 Windows PowerShell 5.1。没有覆盖初次失败记录。

实现经过任务级审查和最终独立审查，未发现阻止交付的问题；最终审查另外执行 25 项运行单元与 Windows CheckOnly 测试，全部通过。这些审查不替代上面的干净构建与真实运行验收。

## 未验证范围与后续工作

Debug、Win32、多机环境、完整 Linux GUI 应用兼容性、中文输入、剪贴板内容正确性、多显示器与缩放、OpenGL 渲染正确性、NSIS 安装包、签名及 CMake 迁移均未在本阶段验收。静态导入检查不能覆盖任意 `LoadLibrary` 路径。

下一阶段按 [维护路线](../maintenance/MAINTENANCE.md) 记录实际 Linux 应用与连接环境，再逐项验证日常使用场景。构建和最小连接通过不代表上述场景已通过。
