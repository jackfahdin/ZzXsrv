# R9.5 PuTTY (Plink) 固定来源补证

日期：2026-09-16。基线：`139380a920f10f06385ef60a85772d394d1b8aee`。
分支：`codex/r9-plink-20260916`，工作区：`D:/File/Program/GitHub/zzxsrv-r9-plink-20260916`。

状态：已于 2026-09-16 取得维护者验收并快进合入 master。本轮为来源补证，无产品代码改动，不需要重建；合入后在 master 重建 dist（构建源码 `139380a92`，与合入后 HEAD 产品代码逐字节相同）上运行 449 项回归通过、0 跳过。分支与工作区保留，未推送。

## 固定提交核验

旧记录（docs/dependencies/SOURCES.md）注明"Web工具未能读取官方commit页，未独立验证远端对象"。本轮克隆官方仓库 `https://git.tartarus.org/simon/putty.git` 至证据目录并完成核验：

- `git cat-file -t 50e360fc7474898b4de0e3533aff50857f2b3289` 返回 `commit`，git 对象哈希自校验，远端对象存在。
- 提交元数据：作者 Jacob Nevins <jacobn@chiark.greenend.org.uk>，作者/提交日期 2025-02-11 23:56:56 +0000，主题 "Docs: belated updates for protocol controls."。
- 与发布 tag 的关系：`git describe --tags` 为 `0.83-24-g50e360fc`，即 0.83 发布点（`d2c178c49a0ae6fa9ef75ca84fb3c9d0d675ea85`）之后第 24 个提交，且为纯文档改动；`git tag --contains` 显示已收录于 0.84 与 0.85。与 `tools/plink/LATEST.VER` 记录的 0.83 一致。
- 上游默认分支现为 `main`（原 master 已重命名）；抓取时 main HEAD 为 `37af6718ff27d97fb97e6831a1cae49ff60ae8d7`（2026-08-26），固定提交至 main 共 160 个提交。

克隆的 remote URL、抓取时间与上述元数据记录在 `.local-validation/r9-plink-20260916/UPSTREAM-COMMIT.txt` 与 `FETCH-TIME.txt`。

## 逐文件比对结论

比对范围：worktree `tools/plink/` 682 个 git 跟踪文件（`git ls-files` 清单、读工作区字节）对上游固定提交树 675 个文件（`git ls-tree -r` 清单、`git cat-file` 读 blob）。结果明细写入 `tools/plink/source-audit.json`（沿用 R9.2 libxcb 的字段结构与分类命名），统计如下：

| 分类 | 数量 |
| --- | --- |
| 逐字节相同 | 669 |
| 仅换行差异（CRLF vs LF） | 0 |
| 内容不同 | 6 |
| 仅本地存在 | 7 |
| 仅上游存在 | 0 |

6 个内容不同的文件均为继承的 VcXsrv/MSVC 构建适配，逐条人工核对 diff（证据 `modified-files-diffs.txt`）：

- `.gitignore`：上游 142 行清空为 0 字节（继承目录不使用上游忽略规则）。
- `defs.h`：删除 `#ifdef NDEBUG` 下的 `#error` 断言保护块（本项目以 NDEBUG 构建）。
- `settings.c`：`qsort`/`sfree` 增加 `(void*)` 强转（MSVC 编译兼容）。
- `ssh/x11fwd.c`：`int socketdatalen=0;` 增加初始化（MSVC 警告/未初始化使用防护）。
- `windows/gss.c`：`SECURITY_WIN32` 定义加 `#ifndef` 保护（与项目 makefile 的 `-DSECURITY_WIN32` 并存）。
- `windows/plink.c`：增加 `#include <fcntl.h>`。

7 个仅本地存在的文件为 VcXsrv mhmake 构建集成：`makefile`、`crypto/makefile`、`otherbackends/makefile`、`ssh/makefile`、`utils/makefile`、`windows/makefile`，以及桩文件 `version.c`（`const char commitid[] = "0";`）。无仅上游存在的文件，即本地树完整覆盖上游固定提交的全部文件。

结论：本地 `tools/plink/` 与上游固定提交高度一致，差异全部可归类为继承适配与构建集成；未发现无法解释的改动，也未发现必须本轮修复的问题。措辞上注意：该目录含上述继承适配，不是纯净上游。

## 上游适用改动评估

固定提交至上游 main 共 160 个提交，其间发布 0.84（2026-05-14）与 0.85（2026-08-14）。完整清单与筛选见证据 `upstream-log.txt` 与 `upstream-applicability.md`。与 plink/SSH 客户端相关的要点（仅评估，不导入）：

- 0.85 含多项协议层安全修复：ETM 模式包解码缓冲区溢出（9d905ca7）、远端 SSH 问候长度限制（d5ed4404）、通道打开拒绝 max packet size 0（e67a7002）、拒绝指数为 1 的 RSA 公钥（b87c33ed）、PPK 加载/保存时校验 Argon2 参数（e489ded4/254f9479），另有 SSH-1 健壮性修复与若干内存泄漏修复。
- 0.84 含加密修复（EdDSA 越界 s 检查 af996b5e、ECDSA 指数约简 7db1f655、ECC 边界 c15d4d7b/65b8f37c、ssh_rsakex 后 sfree 误用 ba3ed53e）、Windows 随机种子指针修复（f9cbe226），以及直接针对 plink/pscp/psftp 等 CLI 工具的 stale-netevent 修复（2eee8848 及跟进 3cbe8522/bb8894ff）、HTTP 代理 digest 多 qop 兼容（580ab260）等。

本地固定版本 0.83+24 早于上述修复。是否升级 PuTTY 属产品决策（涉及版本升级与回归范围），本轮不导入，作为待议事项另行提出。

## 证据与范围

证据目录：`.local-validation/r9-plink-20260916/`（已被 gitignore，未提交进仓库），含上游克隆 `putty/`、`FETCH-TIME.txt`、`UPSTREAM-COMMIT.txt`、`compare-result.json`、`compare_plink.py`、`gen_audit.py`、`upstream-extract/`、`modified-files-diffs.txt`、`upstream-log.txt`、`upstream-applicability.md`。

本轮范围为来源补证：远端对象核验、逐文件比对、适用改动评估与来源文档更新。不执行完整重建的理由：未改动任何产品代码、构建脚本或 `tools/plink` 既有源码，构建输入不变；plink 的编译验证留待产品代码实际变更时进行。PuTTY 版本升级、上游安全修复导入、plink 运行时行为测试均不在本轮范围。

## 下一步

- 维护者决定是否需要及何时升级 PuTTY（0.84/0.85 含上述安全修复），若升级应作为独立任务进行完整构建与回归。
- R9 剩余组件的来源补证继续按本轮方式推进：xserver、xwininfo、mkfontscale、xkbcomp、xkeyboard-config 等仍为"远端对象未独立验证"状态。

## 本版本反馈与主线整合

2026-09-16 维护者验收通过（"没问题，继续吧，验收通过"）。分支 `codex/r9-plink-20260916`（`9645f9c056`）快进合入 master，无冲突；合并差异仅文档与审计文件（SOURCES.md/JSON、本报告、PLAN_STATUS、COMPATIBILITY、validation README 与 `tools/plink/source-audit.json`），无产品代码改动，不需要重建。合入前回归为同日 master 完整重建后的 449 项通过（50.174 秒，构建源码 `139380a92`）；合入后在同一 dist 上重跑 449 项通过（50.084 秒，0 跳过）。整合证据：主仓库 `.local-validation/r9-plink-integration-20260916/`（`merge.log`、`tests-before-merge.log`、`tests-after-merge.log`）。

关于 PuTTY 中文分支（[larryli/PuTTY](https://github.com/larryli/PuTTY)）的来源评估：其 `0.84`/`0.85` 标签与官方上游标签提交完全相同（`cda57ec7`/`a3cce3a4`，逐字节同 SHA），中文版本以 `-cn1` 标签叠加在上游发布之上；`0.85`→`0.85-cn1` 差异 61 个文件、约 2306 行插入，主体是 GUI 资源与消息本地化，但包含 `ssh/common.c`（166 行）、`windows/plink.c`（121 行）等 SSH/客户端代码路径改动。本项目仅随附 plink 命令行工具（XLaunch 后台调用，用户基本不读其控制台输出），本地化收益很小；而改用个人 fork 作为安全敏感组件来源会扩大审计面、引入对 fork 维护者跟进上游安全修复的依赖。结论：后续 PuTTY 升级（R9.6，0.85）仍以官方上游为来源，不采用该中文分支；该结论已经维护者问询后记录。

分支、工作区与证据目录保留，未推送。
