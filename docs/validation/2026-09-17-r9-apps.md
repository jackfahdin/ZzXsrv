# R9.7 xkbcomp / xwininfo / mkfontscale / xkeyboard-config 固定来源补证

日期：2026-09-17。基线 `94d907da77`；分支 `codex/r9-apps-20260916`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r9-apps-20260916`。

状态：已于 2026-09-17 取得维护者验收（"确认"）并快进合入 master。本轮为纯来源补证：四个组件的记录固定提交均已在官方仓库核验存在，并完成逐文件比对；仅新增四个 `source-audit.json` 及来源文档更新，未修改任何产品代码。合入后 452 项回归通过、0 跳过。分支与工作区保留，未推送。

## 固定提交核验

四个组件逐一克隆官方仓库，`git cat-file` 验证记录提交存在（git 对象哈希自校验）：

| 组件 | 记录提交 | 上游元数据 | tag 关系 |
| --- | --- | --- | --- |
| xkbcomp | 继承记录 `9cb80be3bd01bab4eba6a8aea7767c36b2284845` | Pierre Le Marre，2025-01-09，"types: Fix warning for multiple map occurrences" | `xkbcomp-1.4.7-5-g9cb80be`（1.4.7 时代，见下方记录修正） |
| xwininfo | `ab427b9bfd495a7e9a5dc690f5abe5a2859b8de3` | Dan Jacobson，2023-06-08，"Update xwininfo.man for #2" | `xwininfo-1.1.6-1-gab427b9`，已被 1.1.7 收录 |
| mkfontscale | `bcfa27aeef3a9eb720e6b48e3fb8a4c2813fef52` | Alan Coopersmith，2024-03-03，"mkfontscale 1.2.3" | 恰为 tag `mkfontscale-1.2.3` 本身 |
| xkeyboard-config | `595342a90876efb2f60a488b702ef03f90c62e35` | Andrey Butirsky，2024-12-30，"Apple: fix ISO keyboards International English layout" | `xkeyboard-config-2.44-16-g595342a9`，已被 2.45–2.48 收录 |

## 逐文件比对结论

本地跟踪文件对上游固定提交树逐字节比对，明细见各组件 `source-audit.json`（沿用 R9.2/R9.5 的字段结构）：

| 组件 | 逐字节相同 | 仅换行 | 内容不同 | 仅本地 | 仅上游 |
| --- | --- | --- | --- | --- | --- |
| xwininfo（上游 17） | 13 | 0 | 4 | 2 | 0 |
| mkfontscale（上游 21） | 17 | 0 | 4 | 2 | 0 |
| xkeyboard-config（上游 457） | 446 | 0 | 11 | 35 | 0 |
| xkbcomp（对 1.5.0 tag，上游 42） | 30 | 0 | 11 | 23 | 0 |

差异性质全部可解释，无不可解释改动：

- **xwininfo** 4 个：`clientwin.c`（config.h 包含）、`dsimple.c`/`dsimple.h`（unistd.h、Bool 类型 MSVC 适配）、`xwininfo.c`（unistd.h、langinfo `#ifndef _MSC_VER`、本地 `-pause` 选项）。仅本地：config.h、makefile。
- **mkfontscale** 4 个：`hash.h`（strcasecmp 宏）、`hash.c`/`ident.c`（unistd.h、qsort 强转）、`mkfontscale.c`（unistd.h/direct.h、去 `_X_NORETURN` 属性、lstat 防护）。仅本地：config.h、makefile。
- **xkeyboard-config** 11 个：8 个上游符号链接在 Windows 物化（已逐一验证与目标 blob 逐字节相同），3 个 meson 构建适配。仅本地 35 个为 mhmake makefile 系列、build.sh、xkbrules.mak、legacy 批处理及两个 `.in` 命名副本（与上游对应文件逐字节相同）。
- **xkbcomp** 见下节。

## xkbcomp 记录修正

继承记录的旧提交 `9cb80be3` 是 1.4.7 时代历史；本地实际基线是 R8 已导入的官方 1.5.0 发布归档（`docs/validation/2026-09-15-r8-dependencies.md`，归档 SHA-256 与发布公告核对）。本轮独立复核：核验 tag `xkbcomp-1.5.0` 提交 `2c7789785981ad1fce3858c726615b49293f7de0` 在官方仓库存在；对上游 200 个 first-parent 提交全量比对，1.50 tag 以 30/42 文件逐字节相同居首；相对 1.5.0 的其余差异（11 个内容不同、23 个仅本地）全部对应 R8 已记录的 Windows 适配、autotools 生成物与本地构建文件。SOURCES.md 的 xkbcomp 行已由"继承记录未验证"修正为 R8 + R9.7 的双重证据口径，旧记录保留在 JSON 的 previous_record。

## 上游适用改动评估（仅评估，不导入）

- xkbcomp：固定记录点之后 20 个提交含 1.5.0 正式版及多个健壮性修复——本地基线已是 1.5.0，这些修复全部在位；唯一落后的上游提交为纯 CI 配置。无需导入。
- xwininfo：固定点之后 12 个提交止于 1.1.7，无安全修复；含两个内存泄漏修复（低风险正确性改进）与 stdbool 迁移（与本地 Bool 适配方式不同，未来升级需协调）。暂不升级。
- mkfontscale：固定点之后 8 个提交含 1.2.4，无安全修复，仅 `--help`/`--version` 选项微调与 meson 修复。暂不升级。
- xkeyboard-config：固定点之后 121 个提交、4 个正式版本（2.45–2.48），无安全修复（纯键盘数据包，本地只消费生成输出 xkbdata）；固定提交本身的改动曾被上游回退并在 rules 层重做，属版本时效信息。若未来升级应取正式 tag 并回归 xkbdata 产出。

## 证据与范围

证据目录 `.local-validation/r9-apps-20260916/`（不入库），按组件分目录：上游克隆、固定提交核验记录、比对脚本与明细 JSON、差异全文、上游评估。四个 `source-audit.json` 已入库（`third_party/xorg/xkbcomp/`、`third_party/xorg/apps/xwininfo/`、`third_party/xorg/mkfontscale/`、`src/xorg-server/xkeyboard-config/`）。

本轮范围为来源补证：远端对象核验、逐文件比对、适用改动评估与来源文档更新。无产品代码改动，构建与回归不适用；plink 之外的 xkbcomp/xwininfo/mkfontscale 运行时行为本轮未测。xserver、Mesa、pthreads4w 等更大组件的同类补证及 libregex、libwinmain、libtxc_dxtn 的归属澄清不在本轮范围。

## 下一步

候选交付维护者验收。R9 剩余：xserver 固定提交补证（体量大，单独作为 R9.8）及若干嵌套片段的归属澄清。

## 本版本反馈与主线整合

2026-09-17 维护者验收通过（"确认"）。分支 `codex/r9-apps-20260916`（`0d68b7f114`）从基线 `94d907da77` 快进合入 master，无冲突；合并差异仅文档与四个 `source-audit.json`，无产品代码改动，不需要重建。合入前回归为 R9.6 整合时的 452 项通过（48.384 秒）；合入后在主仓库合并顶端对保留的 R9.6 候选运行目录重跑 452 项通过，48.594 秒，均为 0 跳过（dist 构建源码 `e0cad4dbca`，与合并后 HEAD 产品代码逐字节相同）。

主仓库检出合入后干净。整合证据：`.local-validation/r9-apps-integration-20260917/`（`merge.log`、`tests-before-merge.log`、`tests-after-merge.log`、`verification.json`）。分支、工作区与运行目录保留，未推送。

已验收入口不变：`D:/File/Program/GitHub/zzxsrv-r9-putty-20260916/dist/x64/Release/xlaunch.exe`。下一项为 R9.8 xserver 固定提交补证，尚未实施。
