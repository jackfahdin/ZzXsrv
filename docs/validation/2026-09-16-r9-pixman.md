# R9.3 Pixman 0.46.4

日期：2026-09-16。基线 `f142a8e3b1e241f2b8e9a9c64c77106df16213b1`；分支 `codex/r9-pixman-20260916`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r9-pixman-20260916`。

状态：R9.3 已于 2026-09-16 取得“R9.3 正常，合入”的反馈并快进合入 master。Pixman 0.46.4 的完整构建、合入前后各 442 项回归（0 跳过）、候选独立运行和实际 Render 像素检查通过。编译源码 `d9117d088946b0e7a5fa45f19f952c7522d099d3`；5150 个交付文件及摘要不变。本版本实际使用正常，未提供逐项测试明细，未确认范围保留；分支、工作区与新旧运行目录保留，未推送。

## 来源与改动

从[官方发布目录](https://www.cairographics.org/releases/)取得 [Pixman 0.46.4](https://www.cairographics.org/releases/pixman-0.46.4.tar.xz)，下载 SHA-256 为 `a098c33924754ad43f981b740f6d576c70f9ed1006e12221b1845431ebce1239`；SHA-512 与同目录 `.sha512` 文件一致，未独立验证 PGP 签名。完整散列见 `third_party/graphics/pixman/source-files.json`。

归档的 207 个普通文件及 17 个相对符号链接全部纳入；Windows 中将链接物化为目标内容，并在清单保存原链接关系，共 224 文件。218 文件保留上游内容原始字节；6 个源码文件保留继承的 Windows 适配，补丁为 `patches/windows-adaptations.patch`，不把它们描述为本轮新发现的缺陷。另保留三个原生构建/配置文件，总计 227 个当前源码文件记录原始摘要。保留 IN 宏处理、梯形步进宽度、glyph hash 掩码、指针断言及 MMX/SSE2 调整，同时纳入上游 glyph 分配等改动。x64 仍使用 SSE2，未增加新指令集要求。

原目录声明 0.44.3，历史同步 SHA `aafb4caea2f440c8ca9fad2cfa2c2fb9e2779ae6` 未独立证实。与官方 0.44.2 的归档对比记录保留于本地证据，不能视作纯净 0.44.2。原生头和静态库实际报告 0.19.1；本次由官方模板生成 0.46.4 头并同步配置版本字段，保留其他特性宏。

原生构建补入 `pixman-filter.c` 和新版 `pixman-region64f.c`。前者原本已经有公共声明，但旧静态库未编译，正常消费者链接失败；后者是新版小数坐标区域实现。`pixman-region.c` 继续作为模板被包含。Pixman 静态链接进服务器，无独立 Pixman DLL。

四份 NSIS 声明及历史 mkzip 增加 Pixman COPYING、README.vcxsrv.md 两份交付文件；未执行安装卸载。主仓库及已验收 R9.2 目录保留。

## 专项验证

使用 test-driven-development 技能补充实际 MSVC 静态库消费者，断言独立已知的像素和区域结果。9 项测试覆盖版本与公开头一致性、预乘 alpha 混合及行尾保护、A8 遮罩、负源步长、nearest 缩放、目标裁剪、整数区域减法、新小数区域 API，以及卷积滤镜函数链接和实际绘制。前八项各在默认和禁用 SSE2/MMX 的独立进程运行；滤镜测试正常链接真实库并验证输出，均无协议或库行为 mock。

旧 R9.2 库下 9 项中六项通过，三项失败：版本不一致、小数 API 尚不存在及滤镜未链接；总计 5 个失败子项，0.782 秒。新版组件构建后 9 项通过，1.320 秒。前一次 8 项红灯记录也保留，不覆盖历史证据。这里的旧版小数 API 缺失是升级差异，不作为旧版故障。

新工作区的源码基线 433 项执行通过 46 项、缺少产物跳过 387 项，8.403 秒，不能当成完整产品回归。

## 证据与边界

本地证据位于 `.local-validation/r9-pixman-20260916/`，包括官方归档与公开校验文件、继承源码备份、逐文件对照、原生库构建日志、红绿日志、独立审查。完整候选结果见下节。

本轮验证目标是 x64 Release。Win32/MMX、Debug、ARM、RISC-V、全部上游测试、NSIS 安装卸载不在通过范围。R9.2 的反馈不转记为本轮结果；传统字体、OpenGL、双向复制等未确认场景继续保留。

## 完整候选验证

`All -Jobs 16` 于 11:46:24–11:58:14 完成，退出 0。VS2022/MSVC 19.44.35227，SDK 10.0.26100.0，x64 Release；OpenSSL 依赖仍按原流程串行构建。构建环境记录源码 `d9117d088946b0e7a5fa45f19f952c7522d099d3`。构建期间提交 `8371f431b` 只修正补丁 LF 换行、来源清单和说明文档；227 个实际源码/构建配置文件与构建来源提交的原始摘要完全一致。许可证目录中的说明按最终文本核对。

| 验证 | 结果 |
| --- | --- |
| 最终 Pixman 专项 | 9 项通过，1.368 秒 |
| 完整回归 | 442 项通过，0 跳过，49.562 秒 |
| 独立运行检查 | PASS，1.31 秒；33 个 EXE/DLL 架构/依赖、版本、带认证启动、根窗口查询及退出清理 |
| 实际服务器绘制 | 正常认证连接上完成 OVER、A8 遮罩、目标裁剪 3 种 Render 请求，XGetImage 逐像素结果与独立已知值一致；旧 R9.2 对照同样通过 |
| 导入复核 | 207 普通文件及 17 链接关系、224 个上游内容、227 当前文件、补丁和 Git 索引/构建源码摘要一致 |
| 独立审查 | 重算原归档摘要；修正补丁 CRLF 后，干净目录实际重放补丁复现全部 224 个内容；独立 9 项消费者测试通过 |
| 交付目录 | 5150 个实际文件，33 个 EXE/DLL；vcxsrv.exe 与生产输出一致；新增 Pixman COPYING 与来源说明 |
| 既有版本 | master 保持基线且干净；R9.2 的 5148 个交付文件及摘要不变 |

Render 检查使用独立消费者调用实际 libXrender/libX11 和认证服务器，以服务器读回像素证明链路；复用运行检查的进程管理，不替换协议、DLL 或服务端绘制。其源码及日志保留在本地证据 `render-consumer.c`、`verify-render.py`、`render-baseline/`、`render-final/`。常驻回归中已有 9 项 Pixman 消费者测试，此额外产品检查不计入 442 项。

最初来源辅助脚本误将作者邮件地址中的 @ 当成模板占位符，断言后从版本头步骤恢复；另一个辅助脚本对 CRLF 的比较误及一个 PNG 和 Win32 汇编，已从官方归档恢复原始字节，并经独立原归档复核。原继承备份未覆盖。这些辅助脚本问题未改动已验收目录或最终生产算法。

证据包含 `environment-all.json`、`build-all-outcome.json`、`build-all.log`、`tests-pixman-final.log`、`tests-final.log`、`runtime/result.json`、`render-final/result.json`、`portable-files.json`、`verification.json`、`reviewer/final-review.md`。`verify-candidate.py` 检查最终来源、测试、产品和旧目录保护；不将本地正常用例等同于全部上游测试或所有新增 API 场景。

当时候选入口：`D:/File/Program/GitHub/zzxsrv-r9-pixman-20260916/dist/x64/Release/xlaunch.exe`。建议检查启动、gitk、中文、窗口刷新/缩放及关闭后重启。候选交付后取得反馈并完成整合，结果见下节。

## 本版本反馈与主线整合

2026-09-16 维护者反馈“R9.3 正常，合入”。记录为本版本实际使用正常；未提供逐项测试明细，不将传统字体、OpenGL、双向剪贴板或其他未确认场景补记为通过。

候选 `7e88adbaeb10ca6ed9ee9f10a014e9fec2bc0182` 从基线 `f142a8e3b1e241f2b8e9a9c64c77106df16213b1` 快进合入 master，无冲突。合入前 442 项回归通过，50.156 秒；合入后在与 master 相同提交的保留构建工作区运行 442 项回归通过，50.261 秒，均为 0 跳过。随后只更新整合文档。

主仓库原检出中的 179 个文件仅有 CRLF/LF 差异：先逐文件确认除换行外完全等同 HEAD，再保留原始字节备份并同步到 HEAD 原始字节。`git add --renormalize` 后确认索引没有差异，最终 227 个来源文件及补丁摘要完全匹配，工作区干净。没有覆盖独立源码改动。

R9.3 的 5150 个交付文件、R9.2 的 5148 个文件及主目录原有 dist 均保持不变。保留工作区、分支和所有运行目录，未推送。主目录源码已更新，其旧 dist 不会因合入自动重建。

主仓库整合证据：`.local-validation/r9-pixman-integration-20260916/` 中的 `before.json`、`merge.log`、`tests-before-merge.log`、`tests-after-merge.log`、`checkout-byte-differences.json`、`checkout-before/`、`verification.json` 和 `verify-integration.py`。原候选 `verify-candidate.py` 保留合入前基线断言，不用于合入后状态；当前以整合核验为准。

继续使用 `D:/File/Program/GitHub/zzxsrv-r9-pixman-20260916/dist/x64/Release/xlaunch.exe`。下一项为 R9.4 Fontconfig 来源与版本维护，尚未实施。
