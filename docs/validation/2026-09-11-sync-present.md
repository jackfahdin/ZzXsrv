# XSYNC/PRESENT/屏保修复验证

状态：6 个上游补丁已应用，30 项定向回归及独立代码审查通过；完整 All 构建、188 项测试（0 跳过）及独立运行验证通过。**本组已取得使用确认；合入前发现既有认证文件问题，整合暂缓，未合入 master、未 push。**

## 范围与源码

基线为 `b29e3590c87a35b3091e56091ff73424f53ef8b2`，临时分支为 `codex/sync-present-20260911`。生产代码与原生测试提交为 `fda4509c76185a42a87fccf6ed837dfef29f41c4`；候选目录为 `D:/File/Program/GitHub/zzxsrv-sync-present-20260911`。

本组落实[原审计](2026-09-09-xserver-sync-present-audit.md)的 6 项问题：CVE-2025-62229、CVE-2026-34001、CVE-2026-50257、CVE-2026-50260、CVE-2026-50261、CVE-2026-50263。Counter/Fence 销毁的共同补丁覆盖两个问题，另纳入 PRESENT 输出指针的配套修复，共 6 个上游提交；配套修复不额外计为 CVE。

| 范围 | 变更 |
| --- | --- |
| PRESENT 通知 | 按实际添加数量清理失败请求，并将成功创建的通知数组返回调用方 |
| Fence 触发 | 回调删除同组 await 后，从当前表头重新遍历，避免访问已释放的后继 |
| Counter 触发 | 回调后确认预存后继仍在链表中，必要时重新找表头；保留持久 alarm 的正常前进路径 |
| Counter/Fence 销毁 | 回调前更新表头；FreeAwait 清空正在销毁对象中残余节点的触发器指针，销毁循环跳过空指针 |
| 屏保窗口 | 清理旧窗口后重新取得屏幕私有数据，避免使用可能已释放的缓存指针 |

生产代码修改限于 `src/xorg-server/Xext/sync.c`、`Xext/saver.c`、`miext/sync/misync.c` 和 `present/present_notify.c`。保留本地 `int64_t`、`X11_RESTYPE_NONE` 和 Windows 适配。GUI-001 跨屏菜单记录已按维护者决定关闭，不再跟进；此决定不表示代码已修复该现象。

## 来源核验

完整上游 master/stable SHA、实际固定 SHA 下载链接、原作者、下载时间、响应和提取补丁 SHA-256 见 [来源清单 JSON](../dependencies/SOURCES.json) 的本组 6 条记录与 [SOURCES.md](../dependencies/SOURCES.md)。实际补丁从 GitHub 镜像 API 取得，GitLab URL 用于标识原始上游提交。

官方归档 `xorg-server-21.1.24.tar.xz` 的 SHA-256 再次核对为 `1a4eb36ca65cc3b1b936566d677a9786e13c11cd5806e951ac55f3f5ce3984af`。6 组 master/stable SHA 均在官方 ChangeLog 中核实；原始 API 响应和提取补丁哈希符合下载回执。按顺序独立重放补丁后，与最终 4 个生产文件完整一致，73 行非空新增内容与稳定发布源码交叉核对。

重放时路径增加 `src/xorg-server/` 前缀；仅 `f5abfb61` 的未修改上下文需适配：保留本地 `IsSystemCounter(pCounter)` 条件，不为满足补丁上下文额外引入 `pSysCounterInfo` 条件。原始下载补丁保持不变，适配副本另存。完整文件重放一致指本地基线加适配补丁，不代表整个文件等同稳定版。

独立来源审查另将六组 SHA 在各自 ChangeLog 条目内配对，核对压缩包成员与解压文件一致，并从 API 内容重建提取补丁。73 行检查采用去除空白后的逐行存在性检查；完整重放按统一换行后的本地源码文本比较，不声称 CRLF 原始字节或整个稳定版文件一致。

## 自动测试与审查

新增测试直接包含生产 C 翻译单元，使用 MSVC x64 AddressSanitizer。夹具构造本地对象并验证返回值、链表状态、指针及回调次数；实际执行路径采用有类型的依赖夹具，未执行的服务器入口使用立即中止的链接别名。本次测试不启动远程客户端，也不操作用户应用。

| 专项 | 修复前 | 修复后 |
| --- | --- | --- |
| PRESENT 通知 | 9 项中 7 项失败 | 9 项通过 |
| Counter/Fence 生命周期 | 14 项中 6 项失败 | 14 项通过 |
| 屏保私有数据 | 7 项中 1 项失败 | 7 项通过 |
| 合计 | 30 项中 14 项失败，包含 5 个 ASan 释放后访问诊断 | 30 项通过，0 跳过 |

覆盖通知第 1/2/3 项查找或分配失败、成功输出与窗口通知清理、共享 await、空链表、不触发、持久 counter 回调、销毁及屏保成功/失败路径。持久 counter 夹具验证触发后保留节点且遍历终止，不构成完整 alarm 事件协议测试；本地回归不构成网络可利用性结论。

独立审查未发现阻塞问题，另外运行的 30 项定向 ASan 测试通过、编译无警告。审查确认 counter/fence 采用不同遍历策略，销毁与 FreeAwait 修复完整配套，PRESENT 和屏保修复保留本地接口。

新工作区的构建前基线运行 158 项，155 项通过，3 项因 mhmake/字体工具尚未构建而跳过；运行项使用前组 XKB 产物。此记录仅用于基线，不代替本组构建后零跳过验证。

构建后的首次全套测试运行 188 项、57.176 秒，187 项通过、1 项失败：正常目录运行已通过，带空格的临时副本在 30 秒内未取得根窗口查询结果。首次失败日志 `tests-final.log` 保留，原测试自动清理了临时服务器日志，因此不能确定该次超时的根因。

随后按相同“正常目录→缺失 DLL→恢复 DLL 后带空格目录”顺序独立复现，正常目录 1.298 秒、带空格目录 1.233 秒均通过，缺失 DLL 按预期在依赖阶段失败。未修改生产代码、断言或超时值；在完整复跑时仅额外复制临时诊断日志后再按原逻辑清理，188 项全部通过。复跑日志为 `tests-rerun.log`，临时运行证据保存在 `runtime-suite-evidence/`。首次启动超时暂未复现、根因未确认，不能将复跑成功表述为已修复该超时；实际使用时继续留意启动稳定性。

## 构建与运行验证

在候选根目录执行：

```powershell
.\scripts\build\buildall.ps1 -Jobs 30
```

完整 All 构建通过，开始 `2026-09-11T10:37:45.5526329+08:00`，结束 `2026-09-11T10:48:34.5457276+08:00`，退出码 0。构建前后 HEAD 均为 `fda4509c76185a42a87fccf6ed837dfef29f41c4`；构建期间只整理文档，没有改变生产源码或提交 HEAD。

| 检查 | 结果 |
| --- | --- |
| 全套测试（本地工具及真实运行启用） | 188 项通过，0 失败、0 跳过，27.803 秒 |
| 独立运行验证 | 全部步骤通过，1.375 秒；包括依赖、版本、认证启动、根窗口查询及清理 |
| 便携目录 | 5,129 个文件，文件集合与前组一致 |
| 压缩字体 | 4,297 个字体的解压及 PCF 文件头检查通过 |
| 此前已有运行目录中的文件 | 385 个 EXE/DLL 的 SHA-256 保持不变 |

运行验证使用临时认证与独立显示号，仅管理本次启动的子进程，不接触用户剪贴板、应用或防火墙。

首次启动构建后发现提交被测试文件末尾空行检查阻止，在依赖编译阶段停止了本次自有构建进程。修正空行并提交后重新执行完整 All 入口；此前中断日志独立保留，不计为通过的构建。正式构建固定于上述生产提交，结束后检查 HEAD 未变化。

本机详细证据位于候选目录 `.local-validation/sync-present-20260911/`：原始补丁与下载回执、适配重放脚本、修复前后测试、独立审查、完整构建日志、运行结果及便携文件哈希。

## 实际使用确认与整合状态

2026-09-11，维护者针对所交付的独立候选反馈“没问题”，记录为本组实际使用正常。应用版本、连接方式与逐项场景未提供，不补造，也不扩展为所有图形或剪贴板场景通过。

合入前重新运行 188 项测试，53.931 秒，1 项失败。这次正常目录启动后认证持续被拒绝，保存的客户端日志显示 `Invalid MIT-MAGIC-COOKIE-1 key`。对照前组 XKB 与当前产物、验证实际文件字节并分别测试读写函数后，确认已有 Windows 认证文件文本模式问题，详见[认证补充报告](2026-09-11-auth-binary.md)。首次超时日志不完整，不能逐案确定同一原因；后续已不再把相似超时仅作为偶发项放过。

master 仍为 `b29e3590c87a35b3091e56091ff73424f53ef8b2`，没有执行合并或清理。原候选目录 `D:/File/Program/GitHub/zzxsrv-sync-present-20260911/dist/x64/Release` 保留不变；认证补充候选在单独目录构建，仍需自身实际使用确认。
主线 R3 历史 21 项中已整合 12 项，剩余 9 项：本候选覆盖其中 6 项，其余 GLX 属性、字体消费方、RANDR 3 项尚未实施。助手仅做本地提交，push 由维护者操作。

构建证据为 `build-result.json`、`build.log` 和 `environment.json`；全套测试为 `tests-rerun.log`，独立运行结果为 `runtime/result.json`，便携及旧文件对比为 `artifact-verification.json` 和 `old-runtime-comparison.json`。
