# GLX 属性、字体消费方及 RANDR 验证与主线整合

状态：三项分别本地提交，完整构建与运行验证通过；维护者已确认启动、gitk、中文显示及重启正常，合入前后各 350 项测试通过、0 跳过。**已快进合入 master，未 push；传统字体、OpenGL 和双向复制未测试。**临时分支及工作区登记因自动审批阻止清理而保留。

## 范围与提交

延续维护者“三项一起处理，但是分别提交”的要求，从主线 `80d8fe641` 在 `codex/r3-final-20260914` 隔离工作区继续。工作目录为 `D:/File/Program/GitHub/zzxsrv-r3-final-20260914`。

| 项目 | 本地提交 | 行为变化 |
| --- | --- | --- |
| GLX drawable 属性 | `0d01bba12` | 普通与交换字节序入口在读取属性前校验完整请求长度；同步收紧四个相邻请求 |
| X Server 字体消费方 | `c1713a067` | alias 缓冲与 libXfont2 的 1024 字节范围一致；补齐边界、分配及初始化处理；fb/mi 忽略非正字形尺寸 |
| RANDR provider 属性 | `3d373e6c5` | 分配前校验属性总字节数；在新增拒绝路径中释放尚未挂入链表的新属性 |

覆盖[历史适用性表](2026-09-09-xserver-applicability.md)最后三项：CVE-2026-50262、CVE-2026-50256、CVE-2025-49180。字体相邻补丁和 RANDR 本地清理不额外增加历史计数。主线现为 21/21 项已整合；仅表示原 R3 清单的已核实缺口全部完成，不表示全部 X Server 历史问题或其他依赖已完成维护。

本次只修改七个 X Server 生产文件及对应测试，不升级 libXfont2、FreeType、Mesa 等依赖。R4 字体库维护与 R2 真实应用兼容性仍单独跟踪。GUI-001 按维护者原决定保持关闭。

## 来源与适配

完整来源、作者、实际下载地址、master/stable SHA、原始响应和补丁 SHA-256 见[来源 JSON](../dependencies/SOURCES.json)及[来源表](../dependencies/SOURCES.md)。本组包含 1 个 GLX、7 个字体、1 个 RANDR 上游提交。

已逐项核对保存的 GitHub 镜像 API 响应中的完整 SHA，重建对应补丁并与保存内容比较；每组 master/stable 对应均在官方 `xorg-server-21.1.24` ChangeLog 的同一条目内核对。GitLab 补丁端点当时返回 HTML 验证页面，未将其当作补丁证据。上游 URL 标识原始项目，实际来源链接明确指向相同 SHA 的镜像。

官方压缩包 SHA-256 本轮复核为 `1a4eb36ca65cc3b1b936566d677a9786e13c11cd5806e951ac55f3f5ce3984af`。独立来源审查还核对了包内 ChangeLog 与本地文件逐字节一致，以及逐文件上游累计增删行与最终生产差异对应；除已声明 RANDR 清理外无额外生产改动。此检查不声称完整文件顺序重放或整个文件等同上游稳定版。

路径映射增加 `src/xorg-server/`。字体补丁 `e31efd3e` 仅导入 Windows 相关 fb/mi 部分，未导入关闭的 GLAMOR 及上游 `test/pyxtest/meson.build`、`test/pyxtest/test_fb.py`，测试使用本地 native harness；保存的字体补丁哈希范围已经包含路径映射及此裁剪，GLX 补丁保留上游路径，RANDR 补丁为 API 的单文件差异片段。不能将三者的哈希范围混为完整原始提交。

RANDR 上游长度拒绝路径直接返回，在新建属性尚未挂链时留下分配。本地增加 `if (add) RRDestroyProviderProperty(prop)`，采用相邻分配失败分支已有的清理方式；已有属性及其数据保持不变。该补充有独立红／绿回归，另列于 JSON 的本地修正记录，不冒充上游原文。

## 定向测试与审查

本机 Python 及 VS 2022 x64 MSVC 14.44，使用 AddressSanitizer。GLX/alias 测试抽取完整生产函数原文，glyph/RANDR 测试直接包含生产 C 翻译单元，使用真实协议类型和长度宏，不另写判断模型。

| 范围 | 修复前证据 | 本轮修复后 |
| --- | --- | --- |
| GLX | 26 项中 8 项失败 | 26 项通过 |
| 字体消费方 | 33 项中 15 项失败 | 33 项通过 |
| RANDR 上游长度检查 | 原 72 项中 18 项失败 | 原 72 项通过 |
| RANDR 新属性清理补充 | 加入 18 项后，90 项中 18 项失败 | 90 项通过 |
| 最终合计 | 红灯记录分别保留，不重复相加 | 149 项通过，0 跳过 |

本轮重新运行日志为 `glx-resume-green.log`、`font-resume-green.log`、`randr-cleanup-green.log`。独立审查者也运行三组全部 149 项测试并通过，未发现阻塞问题。

覆盖 GLX 正常请求、截断／额外属性数据、计数范围、错误 drawable、普通和交换字节序；字体普通／边界／过长 alias、分配失败、错误或缺失输出、挂起／客户端退出以及正／零／负字形尺寸；RANDR 8/16/32 位属性、替换／前插／追加、current/pending、临界分配、超限拒绝与新属性释放。

测试边界：字体挂起仅验证挂起，不覆盖恢复、递归 alias 和真实字体文件解析；glyph 下游绘制使用夹具。RANDR 使用受限分配器避免巨额分配，正常追加／前插只覆盖等长数据，不声称验证其全部历史累计 size/offset 语义、事件或驱动回调。Windows 构建包含 RANDR 代码，但有效 provider 的运行前置仍未证实。GLX 严格长度检查会拒绝旧源码注释所述古老 Mesa 的额外字节请求，这是本次上游补丁有意取消的兼容行为。

## 完整构建与运行

构建固定来源为 RANDR 提交 `3d373e6c55ddded2984b3bca1fe2c8b27b10681e`，在独立工作区执行完整入口：

```powershell
.\scripts\build\buildall.ps1 -Jobs 30 -EnvironmentReport .local-validation/r3-final-20260914/environment.json
```

完整 All 构建于 `2026-09-14T15:13:33+08:00` 开始、`15:25:53+08:00` 结束，退出码 0。构建前后 HEAD 均为上述完整 SHA；构建过程中只整理文档，未改变生产代码和提交。

| 验证 | 结果 |
| --- | --- |
| 全套测试，启用本地工具和真实运行 | 350 项通过，0 失败、0 跳过，40.301 秒 |
| 独立运行验证 | PASS，1.522 秒，依赖、版本、认证启动、根窗口查询与清理通过 |
| 便携目录实际文件 | 5,129 个，文件集合与前组认证候选一致 |
| 压缩字体 | 4,297 个解压及 PCF 文件头检查通过 |
| 既有运行目录 | 455 个 EXE/DLL 哈希保持不变 |
| 文档与来源 | 103 个本地链接、196 个组件、39 条唯一上游记录核对通过，其中本组新增 9 条 |

构建前基线为 201 项，3 项因新目录中 mhmake/字体工具尚未编译而跳过；构建后的 350 项已覆盖这三项。真实运行测试还保留此前认证接受／拒绝、带空格目录及缺失依赖诊断的回归，不改写之前的历史测试结果。

核心证据为 `build-result.json`、`build.log`、`environment.json`、`tests-final.log`、`runtime/result.json`、`artifact-verification.json`、`old-runtime-comparison.json`、`independent-review.json`、`source-verification.json`、`document-verification.json`。原始日志及哈希清单保存在本机忽略目录，未提交大型构建产物。

所有详细本机证据位于 `.local-validation/r3-final-20260914/`；本轮新增日志保留前次中断前的红／绿记录。

## 实际使用确认与主线整合

2026-09-14，维护者针对上述新目录反馈：“启动正常；gitk 操作正常；中文显示正常；重启正常；传统字体未测；OpenGL 未测。”按所述范围记录为本版本的实际使用确认；双向复制仍保留未测试状态。Linux/应用版本及具体连接参数未提供，不补造，也不扩展成全场景通过，详见[兼容性记录](../maintenance/COMPATIBILITY.md)。

master 从 `80d8fe64154838b172737017189b6848b76a7d50` 快进到 `7fd85601cc5cf7286d5c2f253fd93c8089453b7c`，保留三个独立修复提交，没有 merge commit。合入前 350 项测试通过，40.100 秒；合入后在主目录源码运行 350 项，全部通过，40.385 秒；两次均为 0 跳过。实际运行产物仍指向本组目录，来源固定为 `3d373e6c55ddded2984b3bca1fe2c8b27b10681e`，没有追加生产代码或重复构建。

继续使用 `D:/File/Program/GitHub/zzxsrv-r3-final-20260914/dist/x64/Release/xlaunch.exe`。主目录源码已更新，但主目录旧 dist 不会因快进自动重编译。

清理前登记 45,875 个文件，并核对 5,129 个运行文件的 SHA-256 与交付清单一致。自动审批检查拒绝删除临时工作区根目录 `.git` 指针的操作，仅返回 `blocked by policy`，未给出更具体原因。因此 `codex/r3-final-20260914` 和 Git 工作区登记暂时保留，运行文件不受影响；该目录仍是 worktree，不能描述为已解除登记的普通目录。根指针和登记元数据备份位于主目录整合证据中，未尝试绕过审批限制。

整合证据位于主目录 `.local-validation/r3-integration-20260914/`：合入前测试的副本、`tests-after-merge.log`、`retained-inventory.json`、`retained-verification.json`、文档检查及 `completion.json`。仅本地提交，未 push。
