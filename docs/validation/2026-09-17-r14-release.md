# R14 发布打包：Inno Setup 安装包与免安装 zip

日期：2026-09-17。基线 `337b21798d`；分支 `codex/r14-release-20260917`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r14-release-20260917`。

状态：候选待验收。本轮新增发布打包能力（不动产品代码）：`tools/package_release.py` 从便携运行目录产出两个发布产物——免安装 zip 和 Inno Setup 7 安装包；安装包脚本 `installer/zzxsrv-64.iss`。版本号默认取当天日期（YYYY.M.D），可用 `--version` 覆盖。

## 产物

以 R13 已验收运行目录（`zzxsrv-r13-xserver-20260917/dist/x64/Release`，5152 个文件）为输入：

| 产物 | 大小 | 说明 |
| --- | --- | --- |
| `zzxsrv-2026.9.17-x64-portable.zip` | 49.3 MB | 免安装版，解压即用；zip 完整性校验通过（testzip 无坏条目），文件集与 dist 逐路径一致（5152 = 5152） |
| `zzxsrv-2026.9.17-x64-setup.exe` | 43.2 MB | Inno Setup 安装包（lzma2/ultra64 固实压缩） |

## 安装包设计（对比上游 NSIS 方案的差异）

- **Inno Setup 替代 NSIS**（维护者决定，上游 NSI 脚本保留不删）。
- **admin / 免提权合一**：`PrivilegesRequired=lowest` + `PrivilegesRequiredOverridesAllowed=dialog`——默认当前用户安装（免提权，落到 `%LOCALAPPDATA%\Programs`），用户可在对话框选择整机安装（需管理员）。注册表与快捷方式由 Inno 按安装模式自动落到 HKCU/HKLM，不存在上游 noadmin.patch 的 HKLM 静默写失败问题。
- 开始菜单快捷方式：XLaunch、ZzXsrv (multiwindow)、卸载。
- 许可证页使用仓库根 `COPYING`（GPLv3）；第三方组件许可证随包分发在 `licenses/`。

## 验证

- 静默安装（`/VERYSILENT`，per-user 默认路径改写指到证据目录）：5154 个文件（5152 负载 + 卸载器），exit 0。
- 静默卸载（unins000 `/VERYSILENT`）：exit 0，安装目录清空。
- 安装产物的运行时冒烟：对安装目录跑 `test_*_runtime.py` 4 个模块（21 项）通过。
- 新增 `tools/tests/test_package_release.py` 2 项（zip 镜像 dist 树、缺 dist 拒绝），回归套件 452 → 454 项。
- 整机（admin）安装路径未实测（需要管理员权限的真实安装），对话框分支由 Inno 成熟机制承担，边界保留。

## 证据与范围

证据目录 `.local-validation/r14-release-20260917/`（git 忽略）：安装日志、冒烟日志、安装/卸载后的目录快照。产物输出在 `dist/release/`（不入库）。本轮范围为打包与发布产物：无产品代码改动，dist 运行目录内容不变。

## 下一步

候选交付维护者验收（可双击运行 `dist/release/zzxsrv-2026.9.17-x64-setup.exe` 走一遍正常安装流程，或直接使用 zip）。验收后快进合入 master。GitHub CI 发包的接线（工作流、产物矩阵、签名）待维护者后续给方案，本轮打包脚本已按命令行可调用设计，可直接被 CI 调用。
