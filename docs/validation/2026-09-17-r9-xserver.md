# R9.8 xserver 固定来源补证

日期：2026-09-17。基线 `25c818c61b`；分支 `codex/r9-xserver-20260917`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r9-xserver-20260917`。

状态：已验收并合入 master。本轮为纯来源补证：xserver 记录固定提交已在官方仓库核验存在，并完成 `src/xorg-server` 本体 1674 个本地跟踪文件对上游固定提交树的逐文件比对；仅新增 `src/xorg-server/source-audit.json` 及来源文档更新，未修改任何产品代码。纯来源补证不改变构建输入，dist 产物与合入点无关，无需重建；验收后快进合入即可。

## 固定提交核验

从 https://gitlab.freedesktop.org/xorg/xserver.git 直接按 SHA 浅抓取（GitLab 允许抓取可达任意 SHA，`git fetch --depth 1 origin <sha>`，约 2 分钟），`git cat-file -t` 返回 `commit`（git 对象哈希自校验）：

| 项 | 值 |
| --- | --- |
| 记录提交 | `af86c5cbc2475b410a2edeb0482ac58309296adb` |
| 作者 / 日期 | Enrico Weigelt, metux IT consult；2025-02-26T16:27:34+01:00 |
| 主题 | Xnest: use authorative declarations from X11/XKBlib.h |
| describe | `xorg-server-21.0.99.1-1772-gaf86c5cbc` |
| meson.build 版本 | 21.1.99.1（与既有记录一致） |
| 分支归属 | 位于 master（距 master 顶端 329 个提交）；**不是** server-21.1-branch 或 `xorg-server-21.1.16` tag 的祖先——21.1.16 于 2025-02-25 在 21.1 分支发布，比固定提交早一天，产品版本 21.1.16 不是源码 tag |

抓取时间 2026-09-16T16:57Z（UTC）。另以 `--filter=blob:none --refetch` 补全 master、server-21.1-branch 与 xwayland-*/xorg-server-* tag 的完整历史用于谱系与适用性分析（浅抓取的边界曾截断后续历史抓取，已用 --refetch 修复，细节记入审计 JSON 的 clone_method）。

## 逐文件比对结论

`src/xorg-server` 是混合树。fonts.src（1113 个文件，40 个字体包单独登记）、xkeyboard-config（493，R9.7 已单独核验）、bitmaps（76，xbitmaps 包）、installer（7）不是 xserver 上游内容，单独统计不直接比；`src/xorg-server/xkbdata` 无 git 跟踪文件（构建期生成）。比对主体为 xserver 本体（dix、os、mi、Xext、hw、glx、xkb 等 1674 个本地跟踪文件对上游 1580 个文件），明细与双方向 SHA-256 见 `src/xorg-server/source-audit.json`：

| 区域 | 逐字节相同 | 仅换行 | 内容不同 | 仅本地 | 仅上游 |
| --- | --- | --- | --- | --- | --- |
| xserver 本体（上游 1580） | 1314 | 0 | 263 | 97 | 3 |
| fonts.src（单独登记） | — | — | — | 1113 | — |
| xkeyboard-config（单独登记） | — | — | — | 493 | — |
| bitmaps / installer（单独登记） | — | — | — | 76 / 7 | — |

另对逐字节相同集合抽取 20 个分布样本记录哈希校验（审计 JSON 的 byte_identical_sample_verification）。

### 263 个内容不同文件的分类（类别可重叠）

- **已登记上游补丁触及文件 21 个**：SOURCES.json `applied_upstream_patches` 全部 39 个补丁涉及的 21 个不同文件**全部落在差异集内**（交叉核对无遗漏）；抽查确认补丁块在位（xkb 边界检查、os/io.c 的 BigRequest 长度防护、render/animcur.c 的动画光标计数防护）。
- **hw/xwin fork 改动 65 个**：VcXsrv 对上游 xwin DDX 的长期演进（调试宏、剪贴板、多窗口、WGL 等），另有 46 个 hw/xwin 下仅本地文件（xlaunch、swrastwgl_dri、生成器等）。
- **带 MSVC/Win32 适配标记 95 个**：`_MSC_VER`/`WIN32` 条件块（46 个文件），及其中 51 个文件的 C99 指定初始化器回退（上游 2025 年现代化改动与 MSVC 不兼容，fork 机械回退；类别有重叠）。
- **其余继承适配 112 个**：抽样 20+ 个逐一核对，全部为可解释的 VcXsrv/继承适配（类型与强转修正、特性开关如 Hyper-V 套接字、重新生成的 GLX 间接调度头、本地配置项），无不可解释差异。

仅本地 97 个文件为 mhmake makefile 系列、生成 GLX 头（dispatch.h/glapitable.h/glprocs.h/glfunctions.h）、本地配置头（version-config.h、xkb-config.h、xwin-config.h）与 VcXsrv 专有源码（vcxsrv.sln、system.XWinrc、启动批处理等）。仅上游 3 个文件为 hw/xwin 的 ddraw.h、glx/glshim.c、glx/glthunk.c——VcXsrv 移除了 ddraw 引擎并以自生成包装替代 glshim/glthunk。

## 上游适用改动评估（仅评估，不导入）

完整评估在证据目录 `upstream-applicability.md`（不入库）。`xorg-server-21.1.16..server-21.1-branch` 共 193 个提交：39 个已登记合入、50 个经主题逐条核对已在固定点 master 基线中、104 个本地没有。104 个中与 Windows 构建相关的安全/健壮性修复主要有：2026 批次（dix/colormap 越界读 `8a6e5f0f`、glx 负长度拒绝 `1888711c`、Xi 两组检查 `c9cd39f9`/`ab2766f3`、glyphset 重复字形 UAF `8d604fa1`——后者尚未进入任何 21.1.x 发布）、2025-09/10 加固批次约 24 个空指针/分配失败/越界防护（含 os 日志头越界 `c49bf5f7`）、xkb 无级别名键型序列化修复等；glamor、xfree86、xquartz、dri2 等本地不构建部分不适用（glamor 无 mhmake makefile；miext/rootless 链入 Xwin，相关 4 个修复需功能评估）。另注意 21.1 分支曾回退 5 个 glamor glvnd 提交，其 master 版本仍在本地基线中，因 glamor 不参与 Windows 构建暂无实际影响。上游 master 已于 2026-02-08 关闭并迁至 `main`；固定提交所在 master 线（通往未发布的 21.2）停止演进，后续跟进修复的现实来源是 server-21.1-branch（最新发布 21.1.24）。

## 证据与范围

证据目录 `.local-validation/r9-xserver-20260917/`（git 忽略、不入库）：上游克隆（upstream-xserver/）、比对脚本与明细（compare.py、compare-report.json、classify.py、classify.json）、全部 263 个差异全文（diffs/）、分支评估（assess.py、branch-assessment.json、branch-since-21.1.16.txt）、适用性评估（upstream-applicability.md）。

本轮范围为来源补证：远端对象核验、逐文件比对、已登记补丁交叉核对、适用改动评估与来源文档更新。无产品代码改动，构建与回归不适用；xserver 运行时行为本轮未测。fonts.src 内 40 个字体包的逐包补证、Mesa/pthreads4w 等更大组件的同类补证不在本轮范围。

## 下一步

候选交付维护者验收。本轮收官 R9 来源核验系列的 xserver 项；上述 21.1 分支未合入修复是否启动新一轮补丁整合，由维护者决策。

## 本版本反馈与主线整合

2026-09-17 维护者验收通过（"确认"）。分支 `codex/r9-xserver-20260917`（`c840c43c28`）从基线 `25c818c61b` 快进合入 master，无冲突；合并差异仅文档与 `src/xorg-server/source-audit.json`，无产品代码改动，不需要重建。合入前回归为 R9.7 整合时的 452 项通过（48.594 秒）；合入后在主仓库合并顶端对保留的 R9.6 候选运行目录重跑 452 项通过，49.459 秒，均为 0 跳过（dist 构建源码 `e0cad4dbca`，与合并后 HEAD 产品代码逐字节相同）。

主仓库检出合入后干净。整合证据：`.local-validation/r9-xserver-integration-20260917/`（`merge.log`、`tests-before-merge.log`、`tests-after-merge.log`、`verification.json`）。分支、工作区与运行目录保留，未推送。

已验收入口不变：`D:/File/Program/GitHub/zzxsrv-r9-putty-20260916/dist/x64/Release/xlaunch.exe`。R9 来源核验主体至此收官；剩余为嵌套片段归属澄清（libregex、libwinmain、libtxc_dxtn 等）与 xserver 上游 21.1 分支 104 个未合入提交的适用性评估，后者是否立项由维护者决策。
