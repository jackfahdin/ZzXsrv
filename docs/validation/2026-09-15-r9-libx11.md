# R9.1 libX11 维护

日期：2026-09-15。基线：`bee19868b99f087b86b00ebe34abaef428713bd6`。
分支：`codex/r9-libx11-20260915`，工作区：`D:/File/Program/GitHub/zzxsrv-r9-libx11-20260915`。

状态：2026-09-16 取得“R9.1 正常，合入”的反馈后，已快进合入 master。libX11 1.8.13 的完整构建、合入前后各 425 项回归（0 跳过）和候选独立运行检查通过。构建源码 `f1e9330b0d62078e4a3ed5bdb0b0fecce9f2830d`；5145 个交付文件及摘要不变。本版本实际使用正常，未提供逐项测试明细，未确认范围保留；分支、工作区与运行目录保留，未推送。

## 范围与来源

本轮仅升级 libX11 1.8.11 → 1.8.13。libxcb、Pixman、Fontconfig、PuTTY 保留在 R9 后续轮次。

从 [X.Org 官方归档](https://xorg.freedesktop.org/archive/individual/lib/libX11-1.8.13.tar.xz)取得源码，SHA-256 为 `69606f485c2c07c14ef64f75b7bb326d48587af33795d9ab3e607c0b5f94f11c`，与[维护者发布公告](https://www.mail-archive.com/xorg@lists.x.org/msg08248.html)一致。旧 1.8.11 官方归档 SHA-256 为 `3b74e82943924b45a0b778cc6842976909c3010d9445a8fd185e1dca4d380e88`。未验证 PGP 签名或 Git tag 的提交对应，不把版本声明当成 Git 来源证明。

保留新归档全部 1673 个文件及 26 个本地构建/配置文件。原始及实际字节摘要记录在 `third_party/xorg/libX11/source-files.json`，56 个上游文件的适配记录在 `patches/windows.patch`。继承目录完整保存在忽略证据目录。

## 适配与验证设计

59 个继承差异与旧/新官方文件进行三方合并；`XKBAlloc.c`、`XKBMAlloc.c` 和 `x11.pc.in` 的旧改动已被上游吸收，不重复覆盖。保留 Windows DLL 导出、mhmake、文件/locale 处理、指针宽度转换及 XKB 服务器符号重命名。上游删除的 `src/os2Stubs.c` 不在 Windows 编译清单中。`src/config.h` 中遗留的 1.1.5 元数据同步为 1.8.13。

11 个根目录公共头与组件头内容一致，上游本次没有公共头内容变更，保留当前安装头。四份 NSIS 声明及历史 mkzip 增加 COPYING 和本地来源说明；本轮不运行安装或卸载。

新增五项真实 DLL 消费者检查：Caps Lock 和 Shift 的默认字母类型、默认小键盘、虚拟 Num Lock，以及命名/Unicode 键符转换。测试使用公开头、产品导入库和相邻的交付 DLL。产品未导出 XkbTranslateKeyCode/XkbFreeKeyboard，因此检查已导出初始化 API 返回的公开类型契约；每次进程只保留一个描述符至进程退出。

旧 R8 实际产物对照中，Caps Lock 层级和默认小键盘映射两项失败，另外三项通过，耗时 0.114 秒。初版测试曾引用未导出的 API 并假设初始化会设置 num_types；检查实际公开 API 后改为验证分配容量及类型内容，再确认上述两项真实旧行为失败。没有为升级扩大 DLL 导出集合。

证据目录：`.local-validation/r9-libx11-20260915/`，含官方归档、基线对比、原目录备份、独立合并预期、旧产物对照和后续构建/测试记录。

验证范围为 x64 Release。Win32/Debug、上游完整测试集、NSIS 安装卸载及 GUI 手工操作不自动计为通过。R8 的使用反馈不替代 R9.1 的验收；传统字体、OpenGL、双向剪贴板等未确认项目继续保留未确认状态。

## 最终候选验证

生产源码与测试提交为 `f1e9330b0d62078e4a3ed5bdb0b0fecce9f2830d`。源码阶段共 425 项，46 项执行通过、379 项缺少产物跳过，8.458 秒；这不是完整产品通过。之后执行 `All -Jobs 16`，于 16:56:18–17:08:01 完成，退出 0，使用 VS2022/MSVC 19.44.35227、Windows SDK 10.0.26100.0 和既有原生工具链，OpenSSL 保持串行。构建环境报告记录了实际源码提交；后续仅补充文档，没有生产输入变更。

| 检查 | 结果 |
| --- | --- |
| libX11 消费者回归 | 5 项通过，0.109 秒；旧 R8 对照为 2 项失败、3 项通过；新测试调用本轮交付 DLL |
| 完整实际产物回归 | 425 项通过、0 跳过，43.886 秒；包含原有认证、字体绘制、键盘文件往返等实际消费者 |
| 独立运行检查 | PASS，1.281 秒；33 个 EXE/DLL 架构/依赖、版本、带认证启动、根窗口查询、退出清理 |
| DLL 导出兼容 | 旧 378 个导出全部保留，新 378 个；新增 0，缺失 0 |
| 最终运行目录 | 5145 个实际文件，33 个 EXE/DLL；新增 libX11 的 COPYING 和来源说明，交付 libX11.dll 与生产输出摘要一致 |
| 来源与适配 | 1673 个官方文件及 1699 个导入/本地文件摘要通过，全部 1702 个组件 Git 索引路径和原始字节通过；补丁反向检查通过 |
| 旧版本保护 | 主线保持基线，工作区干净；R8 的 5143 个交付文件及摘要全部不变，其他旧目录保留 |

独立审查逐个对比 59 个预期合并、26 个本地文件、11 个公共头、构建清单和测试契约，未发现阻断项。首次索引复核发现三个继承 man 文件的大小写不同于官方名称，已通过受限的 Git 文件重命名同步；最终路径及字节验证通过。

证据包括 `environment-all.json`、`build-all-outcome.json`、`build-all.log`、`tests-red.log`、`tests-x11.log`、`tests-final.log`、`x11-exports.json`、`runtime/result.json`、`portable-files.json`、`verification.json` 和独立审查的 `reviewer/final-review.md`。`verify-candidate.py` 复核构建与测试结果、实际来源/产物摘要、导出兼容、主线状态及旧 R8 保护。未运行上游全部测试集，不宣称所有键盘或输入法事件路径已被覆盖。

运行入口：`D:/File/Program/GitHub/zzxsrv-r9-libx11-20260915/dist/x64/Release/xlaunch.exe`。已取得本版本使用正常的反馈并合入；继续使用此目录，主目录旧 dist 未重建。R9 后续还有 libxcb、Pixman、Fontconfig、PuTTY 的来源与适用性维护。

## 实际反馈与主线整合

2026-09-16，维护者反馈“R9.1 正常，合入”。master 从 `bee19868b99f087b86b00ebe34abaef428713bd6` 快进至已验收候选 `adfc60bc556ff7c8b73bce0720fc8fd5ae03fedd`，随后仅补充整合文档。

合入前后在 R9.1 已构建工作区分别运行完整实际产物回归，均为 425 项通过、0 跳过，耗时分别为 44.852 秒和 45.078 秒。合入后测试时主线和 R9.1 工作区为同一提交；生产源码和测试未在整合中修改。

主线检出时，1145 个继承文件仍保留旧 CRLF 工作区字节。逐文件确认仅行尾差异、HEAD blob 符合原始摘要后，保存旧字节并同步为提交内容；受限路径重新规范化索引后没有暂存内容差异。这是检出字节同步，没有新源码修正。1699 个组件落盘文件摘要复核通过。

R9.1 的 5145 个交付文件、R8 的 5143 个交付文件及主目录旧 dist 的文件集合和摘要均不变。没有重建或覆盖运行目录，没有删除分支/工作区，没有 push。反馈记录为本版本实际使用正常，不补造逐项人工结果。

整合证据位于主工作区 `.local-validation/r9-libx11-integration-20260916/`，包括 `before.json`、合入前后测试日志、`merge.log`、检出字节差异与备份、`verification.json`。原候选证据保留；原 `verify-candidate.py` 中“主线尚未合入”的断言属于历史候选阶段，整合后使用新的 `verify-integration.py`。
