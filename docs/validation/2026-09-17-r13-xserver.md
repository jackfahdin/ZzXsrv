# R13 xserver 上游 21.1 分支补充批次整合（收官）

日期：2026-09-17。基线 `2316d3eec1`；分支 `codex/r13-xserver-20260917`，独立工作区 `D:/File/Program/GitHub/zzxsrv-r13-xserver-20260917`。

状态：候选待验收。本轮为 R11 评估的收官批次：从 B/C 档中精选有实际价值的 6 个上游提交 cherry-pick（4 个代码修复 + 2 个许可证文本同步），其余 B/C 档评估为不适用或收益不足，明确不再导入。

## 应用结果（6 个全部干净 cherry-pick）

| 上游（server-21.1-branch） | 本地提交 | 内容 |
| --- | --- | --- |
| `bdc7eb8f07f2` | `8aef52db91` | xkb: XkbCopyKeymap 无级别名键型修复（成对第 1 个） |
| `5ebf0a9dd72e` | `81057ac80e` | xkb: 无级别名键型序列化修复（成对第 2 个，单独合入会弄坏 XkbGetNames 回复，本轮已按上游顺序成对应用） |
| `123f50acb34e` | `7d412ef81f` | render: gradient 创建错误路径内存泄漏 |
| `09eb9aa351a6` | `c0aaef1c49` | rootless: Glyphs damage bounding box 取并集计算错误（multiwindow 文本重绘范围） |
| `3069f64d691d` | `9380a82ce7` | COPYING: SGI-B-2.0 补缺失段落（作用于 `src/xorg-server/COPYING`，非仓库根 GPLv3 COPYING） |
| `dd924b160c9b` | `2b8a2eed1a` | COPYING: HPND-sell-MIT 补作者署名（同上） |

6 个提交保留上游作者署名；登记到 `docs/dependencies/SOURCES.md`（R13 批次表）与 `SOURCES.json` 的 `applied_upstream_patches`（58 → 64 条）。

## 未导入项的处置（R11 B/C 档其余 16 个，明确不导入）

- B 档：`c285ed7d`（glx 重复赋值，危害路径被遮蔽）、`c9cd39f9`（gesture 协议校验缺口，无内存安全面）、`c0146196`（仅错误信息取值）、`68c186bf`（需 XACE hook 才触发，本地无安全 hook）、`236e712f`（纯断言加固）、`9848e11d`/`63d6cbf2`（Xinerama+OOM 才触发）、`33eee35e`（FormatInt64 LONG_MIN 边界，MSVC 行为正常且需手工移植到 os/fmt.c）、`d2f1d74f`（多键盘 indicator 同步，Xwin 单键盘场景）、`7cd443c6`（OpenSSL EVP deprecation 清理，仅消除编译告警）。
- C 档：`4a562d37`（触摸路径在 Xwin 为死代码）、`ce300ed2`（GPU screen 在 Xwin 不存在）、`ef781045`/`d7279b8e`（rootless Render 包装，收益面向 macOS 合成器语义，multiwindow 下无明确缺陷对应，无实测收益）。
- 以上判定与 R11 报告一致；如未来上游批次或实际缺陷报告涉及这些路径，可重新评估。

## 构建与回归

- 完整 x64 Release All 构建通过（`buildall.ps1 -Stage All -Jobs 16`），便携运行目录 `dist/x64/Release` 共 5155 个交付文件。
- 452 项回归通过、0 跳过（49.541 秒，含运行时检查），直接针对本候选的新构建运行目录运行。构建/回归源码 `cda929acaafb96d533ec0beb209635ea356458c8`。
- 候选运行目录：`D:/File/Program/GitHub/zzxsrv-r13-xserver-20260917/dist/x64/Release`（入口 `xlaunch.exe`）。

## 证据与范围

证据目录 `.local-validation/r13-xserver-20260917/`（git 忽略、不入库）：6 个上游补丁文件（patches/）、apply 记录、回归日志。本轮范围：上述 6 个提交；未逐项构造触发输入复现（正确性由上游发布与本地代码核对支撑），日常 GUI 场景待维护者实测。

## 下一步

候选交付维护者验收。验收后快进合入 master；本轮合入后 R11 评估台账全部处置完毕（A 档 R12 已合入、B/C 档精选本轮合入、其余明确不导入），xserver 上游跟进转入"新批次出现时另行立项"的常态。
