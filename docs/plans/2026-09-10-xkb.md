# R3 XKB 修复计划

目标：完成[既有审计](../validation/2026-09-09-xserver-xkb-audit.md)列出的 8 项 XKB 问题及 9 个前置、配套提交，提供独立的 x64 Release 候选供实际使用确认。

方案：以固定上游提交为单位作最小回补，保留当前私有头和本地实现结构；逐函数核对 21.1.24 官方发布源码。单独测试真实 C 源码中的边界、正常值和分配失败路径；不重构 XKB，不改输入策略和 GUI-001。

## 全局约束

- 临时分支 `codex/xkb-20260910`，基线 `1a5a0f7e3988eabd36b31d15eb7744be0a14d82f`；本地提交，不 push。
- 候选路径 `D:/File/Program/GitHub/zzxsrv-xkb-20260910`。本组实际使用确认后才能快进合入 master。
- 原始上游标识、实际下载链接和哈希进入依赖来源清单；不建立上游源码分支。
- 不改用户正在运行的程序、不操作剪贴板或系统防火墙。

## Task 1：辅助函数与几何分配

- [x] 为 `XKBGAlloc.c` 的 section doodad 容量、`xkbtext.c` 的 CFile 前缀和 `maprules.c` 的 realloc 失败路径编写真实源码测试，记录修复前失败及正常路径。
- [x] 回补 `dd8b8cf49d326802c53b01835618a7e3765d91cb`、`5dfb435c1d864bf154369cb86d085d4159730378`、`d6c462f59927b3702a54e0e8ea2a5de7639294e6`，只改这三个生产文件和独立测试。
- [x] 通过针对性测试和任务审查。父任务负责提交，实施者不得改其他文件。

## Task 2：请求检查与资源生命周期

- [x] 在真实 `xkb.c` / `xkbEvents.c` 翻译单元上编写回归测试，记录原始失败。覆盖类型数量、层数、描述/映射/保留项和相邻映射区边界、动作数据与零动作、compat 有效计数/上界、几何索引/分配失败及资源释放。
- [x] 按前置顺序回补其余 14 个固定提交，核对调用点和本地 private header，保留已有检查和 skipped 整理语义。
- [x] 通过针对性测试并独立审查最终生产差异及测试。

## Task 3：候选构建与交付

- [x] 更新 `docs/dependencies/SOURCES.json` / `SOURCES.md` 及验证报告，准确区分补丁个数与 8 个 CVE 项目。
- [x] 固定源码提交，运行完整测试、`scripts/build/buildall.ps1 -Jobs 30`、运行包验证和带认证的临时显示运行检查。
- [x] 核对旧运行目录哈希变化，说明验证边界，提交文档并交付候选。此时 master 仍为已验收基线，不自动标记本组人工验收完成。
