# R4 字体依赖升级计划

基线：master `0f121774d`；工作分支：`codex/fonts-20260914`。

## 范围与约束

按现有 R4 维护项升级 libXfont2 2.0.6 → 2.0.9、FreeType 2.13.3 → 2.14.3。分别提交，统一完成 x64 Release 构建和验证，形成独立运行目录供维护者实际验收。主线和旧运行目录保留；不 push。

正式包及下载证据保存在本工作区 `.local-validation/fonts-20260914/`。libXfont2 来自 X.Org 发布目录，FreeType 来自官方网站指定的 Savannah 下载入口。记录最终重定向 URL，不将镜像来源描述为直接下载服务器。

| 组件 | 正式包 SHA-256 |
| --- | --- |
| libXfont2 2.0.9 | `f042a370666815e7b941e9b7019024755bd1c6c2954afbfa515af378251799e2` |
| FreeType 2.14.3 | `36bc4f1cc413335368ee656c42afca65c5a3987e8768cc28cf11ba775e785a5f` |

## 工作项

- [x] 下载并比较新旧正式包与已跟踪文件；基线 350 项测试无失败，3 项因尚未构建本机工具跳过。
- [ ] 引入 libXfont2 正式包变动；保留 `config.h`、mhmake `makefile`、`fsio.c` 的 EWOULDBLOCK 处理和 `bufio.c` 的 intptr_t 转换。移除上游删除且无消费者的 fontencc 文件及编译项；保留现有启用的 fontserver 配置。
- [ ] 接入上游离线 PCF / 字形回复回归测试，测试实际源码而非复制算法；证明旧实现失败、新实现通过，保留命令及日志。
- [ ] 引入 FreeType 正式包变动，保留本地专用文件及现有功能开关；验证新模块在现有 MSBuild 工程中编译。
- [ ] 补充 FreeType 实际加载/栅格化和字体缓存消费者检查；正常 TTF/OTF 与异常输入有可核实结果。
- [ ] 同步组件、嵌套依赖及来源记录，明确本地适配和实际导入范围。
- [ ] 固定代码提交执行 All 阶段完整构建，运行全套测试及独立 runtime 检查；不得以跳过代替相关验证。
- [ ] 独立代码审查并处理重要发现；交付候选目录、测试报告及简明人工测试步骤。

## 验收边界

R3 的人工反馈仅适用于旧候选：启动、gitk、中文显示和重启正常。传统字体、OpenGL、双向复制仍未人工测试。R4 需要新版本实际反馈；自动字体解析测试不能替代 GUI 字体显示验收。GUI-001 已关闭，不重新开展。
