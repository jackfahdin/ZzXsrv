# 项目计划与功能状态盘点

更新日期：2026-09-09。XFIXES 已于整合提交 `a770cb3ff34efa043face0d56c31928b73683d3a` 进入主线，实际程序构建来源 `1e4a1cf51`。对照依赖基线 `9971201a65e6f8ec2d34f9c8d4fbc45479cf9578`、现存工作分支及本机验证记录。

本文件为当前任务状态入口，依据源码、提交和验证记录维护。2026-09-09 维护者已要求继续收尾 XFIXES 与统一状态文档。各项验证以对应报告中的源码、时间和范围为准；旧评估中的目标版本在实施时重新核实。

## 当前结论

先前五份正式实施计划均已完成。本次新增第六份“X Server 输入处理”计划，处于候选待验收：错误返回已作本地终止适配，新适配静态审查、完整构建、71 项测试（0 跳过）及运行验证通过，尚待实际使用确认和主线整合。XFIXES 已取得维护者“已测试，使用正常”的确认并合入 master；合入后 59 项测试通过，0 跳过，175 个既有运行文件哈希不变。GLX 已整合完成，但没有独立命名的实施计划文件，其进度记录在组件评估和验证报告中。

剩余工作包括输入处理候选的实际使用验收与整合、已核实缺失的 X Server 分组修复、日常兼容性补录、其他依赖评估，以及尚未立项的体验和工程方向。XFIXES 收尾及本次状态文档收敛已完成。不能把所有路线图条目都当作已经承诺实施的功能。

## 正式计划逐项结算

| 计划 | 实际状态 | 还剩什么 |
| --- | --- | --- |
| [原生 Windows 构建](superpowers/plans/2026-09-08-native-windows-build.md) | 已完成，相关实现已进入主线 | 该计划内无待办。Debug/Win32、安装包和 CMake 原本就不属于此次交付 |
| [本地维护基线](superpowers/plans/2026-09-09-maintenance-baseline.md) | 已完成，环境报告、运行验证、干净构建和本地标签均有记录 | B1–B6 无剩余。日常 GUI 全场景属于后续阶段 |
| [第一次历史重组](superpowers/plans/2026-09-09-history-restructure.md) | 实施已完成；其中 upstream 的旧定义已被新方案替代 | 不继续按旧方案维护原作者整合快照。归档与原验收记录保留 |
| [依赖基线整理](superpowers/plans/2026-09-09-component-baseline.md) | 已完成并进入主线 | 不需要重做分支结构。组件实际升级是另一项工作 |
| [XFIXES 请求长度校验](superpowers/plans/2026-09-09-xfixes-request-length.md) | 已完成，代码与证据均已合入 master | 本实施计划无剩余；应用场景明细纳入持续兼容性记录 |
| [X Server 输入处理](superpowers/plans/2026-09-09-xserver-input.md) | 候选待验收，来源已导入 upstream，代码仅在候选 | 本地适配、审查、构建和自动验证已完成；剩新候选实际使用确认及主线整合 |

其他 worktree 中同名的计划是旧检出副本，不额外计为新任务。`.local-validation/history-restructure/plan.md` 是第一次历史整理的本机执行材料，也不是新的未完成计划。

## 已完成的功能与工程能力

| 类别 | 已有内容 | 边界 |
| --- | --- | --- |
| 原生构建 | PowerShell 构建入口、工具检查、分阶段执行、原生生成器 | 已验证目标是 Windows x64 Release；参数接受 Debug/Win32 不代表对应组合已验收 |
| 可运行目录 | Portable 文件整理、随程序所需字体和键盘数据 | 已有便携运行目录；没有交付新版 NSIS 安装包或签名发布 |
| 工程诊断 | 构建环境 JSON、依赖扫描、版本及认证启动检查、结果日志 | 这是构建与验证工具，不等于已改进终端用户的日志入口或错误界面 |
| GLX 修复 | 修复已整合进 master，有干净构建、51 项测试和用户“能用”反馈 | 不等于所有 X Server 问题均已排查，也不等于完整 OpenGL 场景覆盖 |
| XFIXES 修复 | 已合入 master，实际使用正常，合入后 59 项测试通过 | 具体 GUI 场景仍缺明细，不等于全部 X Server 修复已完成 |
| 构建过程修正 | 字体配置生成代码的 Windows 路径转义、OpenSSL 构建串行化 | 是构建问题修复；不是 FreeType/Fontconfig/OpenSSL 版本升级 |
| 仓库维护 | 历史归档、来源说明、依赖树清单、upstream/master 两层关系 | 新 upstream 是现有依赖来源基线，没有自动把全部组件升级到新版本 |

原有 XLaunch、配置加载、剪贴板、窗口、键盘等 VcXsrv 功能属于继承的产品能力。后续“使用体验”阶段应针对实际问题改进这些能力，不能把它们描述成尚需从零开发，也不能因其存在就视为已经全面验证。

## 真正剩余的工作

原 R1（XFIXES 收尾）和 R10（状态文档收敛）已完成；保留其余编号便于追踪。

| 编号 | 工作 | 当前状态 | 具体完成条件 |
| --- | --- | --- | --- |
| R2 | 日常兼容性清单 | 有笼统可用反馈，逐项证据不足 | 固定程序目录、Linux/应用版本与连接方式，记录启动/关闭/重启、GUI、双向文本剪贴板、中文显示与输入、窗口、多显示器/缩放、OpenGL 的结果；无条件执行的项目写明未验证 |
| R3 | 其余 X Server 修复核对与分组实施 | [本轮适用性表](validation/2026-09-09-xserver-applicability.md)已完成；21 项 CVE 缺关键源码修复，含输入候选的 2 项 | 先完成[输入候选](validation/2026-09-09-xserver-input.md)实际使用确认与整合，再分组处理 RENDER/RECORD、XKB、XSYNC/PRESENT/屏保、GLX 属性、字体消费方和 RANDR；每组单独验证和验收 |
| R4 | 字体链路 | 已评估消费者，尚未升级 | 对 libXfont2、FreeType 的相关改动逐项核对，选定版本或补丁后验证字体解析、中文显示及缓存；构建生成器修正不替代本项 |
| R5 | libxml2 来源与重建 | 来源未闭合，仍使用预编译库 | 确认源码、版本、补丁和构建方式，再评估替换；联动 XLaunch 配置与 Fontconfig 等消费者验证 |
| R6 | Expat | 已评估，尚未升级 | 核对实际使用配置并更新、重建其 Mesa 消费者；不要求同时整体升级 Mesa |
| R7 | OpenSSL 版本维护 | 已评估 LTS 迁移，尚未升级 | 复核目标版本及实际 API 兼容性，独立导入和验证；当前源码仍为 3.4.1 |
| R8 | xkbcomp、libXpm、zlib | 已列候选，尚未逐项实施 | 分组件决定是否需要更新或回补，并验证对应键盘、图标、压缩及字体链路 |
| R9 | 其余依赖与来源清单 | 深度不齐，未完成全面核对 | 继续核对 libX11、libxcb、Pixman、Fontconfig、PuTTY 等；对不在初始依赖集合的 libxml2、libregex、libwinmain 等分别澄清来源或归属，不假定它们都能对应一个纯净组件仓库 |

R3 已有逐项源码缺口证据，但整项修复尚未完成；R4–R9 仍是维护待评估项，不表示所有列出的组件都已经确定需要升级。具体版本和补丁须在执行时确认，不能照抄早期评估表直接替换。

## 暂缓或尚未立项的方向

| 方向 | 目前定位 |
| --- | --- |
| 启动配置、用户日志入口、错误提示、中文说明改进 | 路线图第三阶段方向；尚无逐项问题清单、设计和实施计划。原有功能仍然存在 |
| 单组件 CMake 试验 | 独立工程方向，尚未执行；libxml2 重建可以成为候选切入点，但未决定整仓迁移 |
| Mesa 整体升级 | 早期评估明确暂缓，等待实际问题或明确收益；相关小补丁和 Expat 更新可独立处理 |
| Debug、Win32、多机环境 | 当前验收范围之外；有需求时另立验证范围 |
| 安装包、签名、版本标识与正式发布 | 尚未实施，不属于已完成 Portable 整理的欠交部分 |
| 远程推送、CI、异地备份 | 当前本地维护约定之外，未配置；本地归档已经存在，但不等于异地备份 |
| 自动依赖更新或定时监控 | 没有启用；维护规则不是后台自动执行任务 |

## 已识别并修正的混淆（保留原因供后续维护）

1. **“当前基线”同时指不同年代的产物。** [维护路线](MAINTENANCE.md) 原先称旧 `a4adc3dc3` 为当前已验证基线，现已改为“最初基线”，后文又记录 GLX 已合入。应区分最初基线、主线当前功能和待验收候选。旧标签无需移动。
2. **早期评估被当作实时待办。** [组件评估](validation/2026-09-09-component-update-assessment.md) 固定于 `0d102004d`，仍写 GLX 补丁尚未取得、下一步先做 GLX。这是当时的事实，后来的 [GLX 报告](validation/2026-09-09-glx-context-tags.md) 已记录完成；不能重复执行。
3. **路线图漏掉了正在收尾的候选。** 原先候选报告未提交，主线看不到进展。现已把候选计划、报告和入口纳入主线，明确文档可见不代表代码合入。
4. **候选文档延续旧分支定义。** XFIXES 原计划和报告使用过“upstream 保存原作者整合快照”的旧定义；现已修正为依赖来源集合，并保留当时分支检查的历史语境。
5. **测试数字对应不同版本和不同检查种类。** 初次原生构建的 21 项、初始基线的 43 项、GLX 的 51 项、XFIXES 的 59 项是不同源码阶段。分支整理时的 304/305 项主要是对象、引用、链接、文件哈希断言，不是数百项新增产品功能测试。
6. **“完整构建”与“依赖全部从源码构建”混用。** All 阶段已验证，但 libxml2 仍使用仓库中的预编译库；来源重建工作没有因此完成。
7. **已有日志与计划中的日志体验混用。** 当前已有构建和测试日志；面向普通用户的日志入口、提示改善尚未有具体实施范围。
8. **旧工作分支的存在被误解成未完成。** history-restructure、update-xserver-security 已在 master 历史中；maintenance-baseline 属于重组前提交，其内容已按映射迁入，不能因提交号不直接连通就当成漏合并。

## 分支、文档与运行版本对照

| 对象 | 当前含义 | 不应进行的推断 |
| --- | --- | --- |
| master（功能整合 `a770cb3ff`） | GLX、XFIXES、构建修正、依赖基线与维护文档；生产文件与 XFIXES 干净构建来源 `1e4a1cf51` 相同 | 主线前进不代表主目录 dist 自动重建 |
| upstream：`7f8950836` | 从 `9971201a6` 顺序导入输入处理的三笔原始上游补丁；尚未整合到 master | 主线清单仍对应已整合的旧快照，候选清单对应新快照；来源导入不等于程序已验收 |
| codex/update-xserver-input（固定源码 `c06e0db16`） | 候选待验收；完整构建与 71 项测试通过，已交付新运行目录 | 自动检查不替代新版本实际使用确认；未合入 master |
| codex/update-xfixes-security | 已合入；分支与工作区保留以追溯原验证材料 | 其存在不表示仍有未合入功能；XFIXES 已有自己的用户确认 |
| 其余旧工作分支和 archive | 先前工作的留存或归档 | 本次不清理，不将其重复列为开发待办 |

最初盘点发现 XFIXES 工作区有四处未提交文档：`docs/UPSTREAM.md`、其实施计划、`docs/validation/README.md` 为已修改；`docs/validation/2026-09-09-xfixes-request-length.md` 为未跟踪。继续处理时已将原内容备份至本机 `.local-validation/plan-followup-20260909/candidate-before/`，报告与计划现已整理提交、修复已合入；接续和验收状态见其报告。

本机运行目录分别为：

- 初始基线：`D:/File/Program/GitHub/vcxsrv-baseline-20260909-121720/dist/x64/Release`，来源 `a4adc3dc3`。
- 已有用户可用反馈的 GLX 版本：`D:/File/Program/GitHub/vcxsrv-glx-20260909-1625/dist/x64/Release`，来源 `5f6c9a930`。
- 已确认可用并合入的 XFIXES 版本：`D:/File/Program/GitHub/vcxsrv-xfixes-20260909-1800/dist/x64/Release`，来源 `1e4a1cf51`；历史 summary 为 PASS，tests.log 为 59 项通过。
- 输入处理候选：`D:/File/Program/GitHub/vcxsrv-input-20260909-2110/dist/x64/Release`，来源 `c06e0db16`；完整自动验证 PASS，实际使用待确认。初版 `vcxsrv-input-20260909-2130` 仅源码检出，不是运行候选。
- 主仓库 `dist/x64/Release` 属于保留的既有产物，本次没有把它认定为最新 master 的重建结果。也未查询用户当前正在运行哪个目录的程序。

## 建议的收敛顺序

状态入口、XFIXES 收尾和本轮 X Server 适用性核对均已完成。下一步先完成输入候选的实际使用确认与整合，再继续分组修复和其他依赖维护；同时按 [兼容性记录](COMPATIBILITY.md) 补齐实际使用场景。体验改善、CMake、Mesa 整体升级和发布工程保留为独立方向，不插入当前修复任务。

今后的状态表统一使用“已合入”“开发草稿”“候选待验收”“待评估”“未立项”“暂缓”“历史已替代”，每项同时记录来源提交、验证报告和剩余动作。这样“代码写完”“构建通过”“用户可用”“合入主线”就不会再合成一个含糊的“完成”。
