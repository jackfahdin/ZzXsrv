# 项目计划与功能状态盘点

更新日期：2026-09-10。仓库现仅保留线性 `master`，23 个首父步骤按 tree 无损重建；重建终点 `b8be16d` 与整理前 `a296b1553` 的文件树完全相同，随后提交本轮文档整理。GLX、XFIXES 仍已合入，既有程序和验证结果没有因历史重建而重新生成。

本文件为当前任务状态入口。本轮只整理仓库、来源文档和远程首次推送，不推进功能或候选验收。`origin` 为 `git@github.com:jackfahdin/ZzXsrv.git`，本轮普通首次推送的结果见 [仓库整理报告](validation/2026-09-10-repository-reorganization.md)。各项历史验证仍以对应报告中的源码、时间和范围为准。

## 当前结论

先前五份正式实施计划均已完成。第六份“X Server 输入处理”计划，处于候选待验收：错误返回已作本地终止适配，新适配静态审查、完整构建、71 项测试（0 跳过）及运行验证通过，尚待实际使用确认和主线整合。XFIXES 已取得维护者“已测试，使用正常”的确认并合入 master；合入后 59 项测试通过，0 跳过，175 个既有运行文件哈希不变。GLX 已整合完成，但没有独立命名的实施计划文件，其进度记录在组件评估和验证报告中。

剩余工作包括输入处理候选的实际使用验收与整合、已核实缺失的 X Server 分组修复、日常兼容性补录和其他依赖评估。本轮完成来源目录盘点并明确未知项，不代表依赖已经升级；R2–R9 范围不扩大。体验和工程方向仍需另行立项。

## 正式计划逐项结算

| 计划 | 实际状态 | 还剩什么 |
| --- | --- | --- |
| [原生 Windows 构建](superpowers/plans/2026-09-08-native-windows-build.md) | 已完成，相关实现已进入主线 | 该计划内无待办。Debug/Win32、安装包和 CMake 原本就不属于此次交付 |
| [本地维护基线](superpowers/plans/2026-09-09-maintenance-baseline.md) | 已完成，环境报告、运行验证、干净构建及当时标签均有历史记录 | B1–B6 无剩余。旧标签已归档后删除；日常 GUI 全场景属于后续阶段 |
| [第一次历史重组](superpowers/plans/2026-09-09-history-restructure.md) | 历史实施已完成，分支方案已被 2026-09-10 单主线规则替代 | 归档与原验收记录保留，不重做旧分支方案 |
| [依赖基线整理](superpowers/plans/2026-09-09-component-baseline.md) | 历史实施已完成，来源改由文档清单维护 | 组件实际升级是另一项工作，不恢复 upstream 分支 |
| [XFIXES 请求长度校验](superpowers/plans/2026-09-09-xfixes-request-length.md) | 已完成，代码与证据均已合入 master | 本实施计划无剩余；应用场景明细纳入持续兼容性记录 |
| [X Server 输入处理](superpowers/plans/2026-09-09-xserver-input.md) | 候选待验收，未合入 master；源码和补丁已归档 | 本地适配、审查、构建和 71 项自动验证已完成；实际使用确认及主线整合仍待后续授权任务 |

11 个旧 worktree 已解除登记，原目录中的计划只是保留副本，不额外计为新任务。旧提交、标签和分支名只用于关联归档证据；当前已不保留这些引用。

## 已完成的功能与工程能力

| 类别 | 已有内容 | 边界 |
| --- | --- | --- |
| 原生构建 | PowerShell 构建入口、工具检查、分阶段执行、原生生成器 | 已验证目标是 Windows x64 Release；参数接受 Debug/Win32 不代表对应组合已验收 |
| 可运行目录 | Portable 文件整理、随程序所需字体和键盘数据 | 已有便携运行目录；没有交付新版 NSIS 安装包或签名发布 |
| 工程诊断 | 构建环境 JSON、依赖扫描、版本及认证启动检查、结果日志 | 这是构建与验证工具，不等于已改进终端用户的日志入口或错误界面 |
| GLX 修复 | 修复已整合进 master，有干净构建、51 项测试和用户“能用”反馈 | 不等于所有 X Server 问题均已排查，也不等于完整 OpenGL 场景覆盖 |
| XFIXES 修复 | 已合入 master，实际使用正常，合入后 59 项测试通过 | 具体 GUI 场景仍缺明细，不等于全部 X Server 修复已完成 |
| 构建过程修正 | 字体配置生成代码的 Windows 路径转义、OpenSSL 构建串行化 | 是构建问题修复；不是 FreeType/Fontconfig/OpenSSL 版本升级 |
| 仓库维护 | 线性 master、旧历史归档、固定来源清单与证据边界、已配置 origin | 来源盘点不是组件升级；旧工作目录保留为普通目录，不再是 worktree |

原有 XLaunch、配置加载、剪贴板、窗口、键盘等 VcXsrv 功能属于继承的产品能力。后续“使用体验”阶段应针对实际问题改进这些能力，不能把它们描述成尚需从零开发，也不能因其存在就视为已经全面验证。

## 真正剩余的工作

原 R1（XFIXES 收尾）和 R10（状态文档收敛）已完成；保留其余编号便于追踪。

| 编号 | 工作 | 当前状态 | 具体完成条件 |
| --- | --- | --- | --- |
| R2 | 日常兼容性清单 | 有笼统可用反馈，逐项证据不足 | 固定程序目录、Linux/应用版本与连接方式，记录启动/关闭/重启、GUI、双向文本剪贴板、中文显示与输入、窗口、多显示器/缩放、OpenGL 的结果；无条件执行的项目写明未验证 |
| R3 | 其余 X Server 修复核对与分组实施 | [本轮适用性表](validation/2026-09-09-xserver-applicability.md)已完成；21 项 CVE 缺关键源码修复，含输入候选的 2 项 | 先完成[输入候选](validation/2026-09-09-xserver-input.md)实际使用确认与整合，再分组处理 RENDER/RECORD、XKB、XSYNC/PRESENT/屏保、GLX 属性、字体消费方和 RANDR；每组单独验证和验收 |
| R4 | 字体链路 | 已评估消费者，尚未升级 | 对 libXfont2、FreeType 的相关改动逐项核对，选定版本或补丁后验证字体解析、中文显示及缓存；构建生成器修正不替代本项 |
| R5 | libxml2 来源与重建 | 来源未闭合，仍使用预编译库 | 确认源码、版本、补丁和构建方式，再评估替换；联动 XLaunch 配置与 Fontconfig 等消费者验证 |
| R6 | Expat | 已评估，尚未升级 | 核对实际使用配置并更新、重建其 Mesa 消费者；不要求同时整体升级 Mesa |
| R7 | OpenSSL 版本维护 | 已评估 LTS 迁移，尚未升级 | 复核目标版本及实际 API 兼容性，独立导入和验证；当前源码仍为 3.4.1 |
| R8 | xkbcomp、libXpm、zlib | 已列候选，尚未逐项实施 | 分组件决定是否需要更新或回补，并验证对应键盘、图标、压缩及字体链路 |
| R9 | 其余依赖与来源清单 | [来源目录盘点](DEPENDENCY_SOURCES.md)已完成；部分精确来源仍未知，未执行升级 | 沿清单继续补证 libX11、libxcb、Pixman、Fontconfig、PuTTY 等的固定来源和适用改动；libxml2、libregex、libwinmain 等按各自证据澄清归属，不假定都能对应纯净组件仓库 |

R3 已有逐项源码缺口证据，但整项修复尚未完成；R4–R9 仍是维护待评估项，不表示所有列出的组件都已经确定需要升级。具体版本和补丁须在执行时确认，不能照抄早期评估表直接替换。

## 暂缓或尚未立项的方向

| 方向 | 目前定位 |
| --- | --- |
| 启动配置、用户日志入口、错误提示、中文说明改进 | 路线图第三阶段方向；尚无逐项问题清单、设计和实施计划。原有功能仍然存在 |
| 单组件 CMake 试验 | 独立工程方向，尚未执行；libxml2 重建可以成为候选切入点，但未决定整仓迁移 |
| Mesa 整体升级 | 早期评估明确暂缓，等待实际问题或明确收益；相关小补丁和 Expat 更新可独立处理 |
| Debug、Win32、多机环境 | 当前验收范围之外；有需求时另立验证范围 |
| 安装包、签名、版本标识与正式发布 | 尚未实施，不属于已完成 Portable 整理的欠交部分 |
| CI、产品发布、归档异地备份 | 本轮已配置 origin 并安排普通首次源码推送；CI、产品发布和本地归档的异地备份仍未实施 |
| 自动依赖更新或定时监控 | 没有启用；维护规则不是后台自动执行任务 |

## 当前主线、归档与运行版本

| 对象 | 当前状态 | 使用边界 |
| --- | --- | --- |
| master | 唯一保留的本地工作分支；包含 GLX、XFIXES、构建修正与维护文档 | 历史重建的等树校验不代表重新构建程序 |
| 旧引用和提交 | 已归档；upstream、旧工作分支及旧 tags 已删除 | 通过[整理报告](validation/2026-09-10-repository-reorganization.md)查映射和归档，不再按旧名称操作 |
| 输入候选，历史源码 c06e0db16 | 71 项自动验证通过，实际使用未确认，未合入 master | 源码和运行文件保留在普通目录；补丁见下文，不能把旧目录当作 worktree |
| 11 个旧工作目录 | 已解除 Git worktree 登记，源码和运行文件在原路径保留 | 不对这些目录运行 git -C 并假定访问的是原检出 |

输入候选保存在原普通目录，并另存于本机忽略目录的 `.local-validation/reorganize-20260910/pending-input-source.patch` 和 `.local-validation/reorganize-20260910/current-development.bundle`。本轮未重新测试、合入或放弃此候选；后续恢复开发应先查整理报告中的来源与恢复方法。

历史测试数字对应各自源码阶段：初始基线 43 项、GLX 51 项、XFIXES 59 项、输入候选 71 项。它们均不代表本轮重新运行的产品测试。旧报告中提到的分支和标签属于当时语境，当前状态以本页为准。

本机运行目录分别为：

- 初始基线：`D:/File/Program/GitHub/vcxsrv-baseline-20260909-121720/dist/x64/Release`，来源 `a4adc3dc3`。
- 已有用户可用反馈的 GLX 版本：`D:/File/Program/GitHub/vcxsrv-glx-20260909-1625/dist/x64/Release`，来源 `5f6c9a930`。
- 已确认可用并合入的 XFIXES 版本：`D:/File/Program/GitHub/vcxsrv-xfixes-20260909-1800/dist/x64/Release`，来源 `1e4a1cf51`；历史 summary 为 PASS，tests.log 为 59 项通过。
- 输入处理候选：`D:/File/Program/GitHub/vcxsrv-input-20260909-2110/dist/x64/Release`，历史来源 `c06e0db16`；完整自动验证 PASS，实际使用待确认。该目录和初版 `vcxsrv-input-20260909-2130` 均已是普通保留目录，后者只保留源码，没有运行候选。
- 主仓库 `dist/x64/Release` 属于保留的既有产物，本次没有把它认定为最新 master 的重建结果。也未查询用户当前正在运行哪个目录的程序。

## 建议的收敛顺序

本轮止于仓库和文档整理，不继续输入候选或其他功能工作。后续维护可先恢复输入候选并取得实际使用确认，再决定主线整合与分组修复；兼容性补录仍见 [兼容性记录](COMPATIBILITY.md)。体验改善、CMake、Mesa 整体升级和产品发布保留为独立方向。

今后的状态表统一使用“已合入”“开发草稿”“候选待验收”“待评估”“未立项”“暂缓”“历史已替代”，每项同时记录来源提交、验证报告和剩余动作。这样“代码写完”“构建通过”“用户可用”“合入主线”就不会再合成一个含糊的“完成”。
