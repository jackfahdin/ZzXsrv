# 日常使用兼容性记录

更新日期：2026-09-10。任务状态见 [PLAN_STATUS.md](PLAN_STATUS.md)。本表记录实际操作结果，不把自动启动成功或笼统的“能用”换算成全部场景通过。已补入维护者对输入处理候选的实际使用反馈；下列源码号为对应构建来源。

## 已有反馈

| 版本 | 已记录的实际反馈 | 范围 |
| --- | --- | --- |
| 初始基线，源码 a4adc3dc3 | 维护者反馈“我刚刚验证了，没问题” | 应用和操作明细未记录 |
| GLX 版本，源码 5f6c9a930 | 维护者反馈“能用” | 应用和操作明细未记录 |
| XFIXES 版本，源码 1e4a1cf51 | 维护者明确反馈“已测试，使用正常” | 确认的是所列候选目录；应用和操作明细未记录，自动验证见 [报告](../validation/2026-09-09-xfixes-request-length.md) |
| 输入处理候选，源码 c06e0db16 | 已确认测试目录；常用操作正常，菜单跨屏异常暂缓排查 | 逐项结果及未验证范围见下文；修复已按当前目录恢复并合入 master |

## 已确认 XFIXES 版本的逐项场景

运行目录：`D:/File/Program/GitHub/vcxsrv-xfixes-20260909-1800/dist/x64/Release`。原 worktree 已解除登记，目录和运行文件保留，XFIXES 代码仍已合入 master。

| 场景 | 人工结果 | 需要记录的内容 |
| --- | --- | --- |
| 启动、关闭、再次启动 | 未验证 | 操作及是否正常退出；是否影响再次连接 |
| 常用 Linux GUI | 未验证 | 应用名称、版本、启动与基本操作 |
| 双向文本剪贴板 | 未验证 | Linux 到 Windows、Windows 到 Linux 分别记录 |
| 中文显示 | 未验证 | 应用、字体、是否缺字或乱码 |
| 中文输入 | 未验证 | 输入法、应用、输入及提交文字的行为 |
| 多窗口与键盘 | 未验证 | 切换、焦点、快捷键及窗口关闭 |
| 多显示器与缩放 | 未验证 | 显示器数量、缩放值、移动窗口情况 |
| OpenGL 应用 | 未验证 | 应用名称、渲染、切换和退出情况 |
| 断开与重连 | 未验证 | 正常关闭客户端或连接后重连的行为 |

每次反馈同时记录：程序目录或来源提交、Linux 发行版、连接方式、应用版本，以及与场景相关的 Windows 显卡/驱动、显示器和缩放信息。尚未提供的信息写“未记录”；没有对应环境的场景写“未验证”并说明原因，不视为失败。

维护者已确认此版本使用正常并完成主线整合；未提供的逐项场景继续补录，不把缺少场景明细等同于已确认有故障。自动依赖检查、认证连接和测试数量仍以候选报告为准。

## RENDER/RECORD 的实际使用反馈

2026-09-10，维护者针对本组候选反馈“没问题”，记录为实际使用正常，修复已快进合入 master，合入后 97 项测试通过。运行目录为 `D:/File/Program/GitHub/zzxsrv-render-record-20260910/dist/x64/Release`，现为普通保留目录，见[验证与整合报告](../validation/2026-09-10-render-record.md)。应用版本、连接方式与逐项场景尚未提供，不补造细节，不扩展为剪贴板/OpenGL 等全部通过；GUI-001 继续暂缓。

## 输入处理候选的实际使用反馈

运行目录：`D:/File/Program/GitHub/vcxsrv-input-20260909-2110/dist/x64/Release`，历史源码 `c06e0db1669afaee649bdaf407c7c76c9fa3ae56`。2026-09-10 维护者明确确认本次使用的是该候选，而非主目录重新编译的版本。历史新适配静态审查、干净构建、71 项测试和认证启动/根窗口检查已通过；本段记录的是旧候选的人工反馈。随后修复已按当前目录恢复并合入 master，新构建和 73 项测试见[整合报告](../validation/2026-09-10-xserver-input-integration.md)；不把旧候选反馈改写成新构建的全场景人工验收。

| 场景 | 人工结果 | 证据边界 |
| --- | --- | --- |
| 程序打开、关闭、再次打开 | 正常 | 不等同于 SSH 断开重连或 X Server 重启验证 |
| 窗口拖动、缩放、菜单、键盘输入 | 基本操作正常 | 已打开菜单随主窗口跨屏移动存在下述异常；中文输入未单独验证 |
| 同时使用多个窗口 | 正常 | 维护者反馈没有卡死、闪退或断连 |
| Windows/Linux 双向文本剪贴板 | 未验证 | 维护者尚未完成此项测试；SSH 连接方式本身不能判断是否具备 X11 剪贴板测试条件 |
| OpenGL 应用 | 未验证 | 维护者未提供正在使用的 OpenGL 测试程序 |
| 中文显示、中文输入、SSH 断开重连 | 未验证 | 本次未提供专项结果 |
| 多显示器菜单行为 | 异常，暂缓排查 | 见 GUI-001；不能记作多显示器全面通过 |

使用背景：维护者表示平常通过 SSH 连接 Linux 并使用 gitk，Windows 有两个显示器。Linux 发行版、gitk/Tk 版本、SSH 的 X11 转发设置、显示器缩放值，以及异常窗口究竟属于远程还是本机 gitk，均未记录或未确认。

### GUI-001：主窗口跨屏移动后，已打开菜单留在原屏幕

- 复现描述：在 A 显示器打开 gitk，展开菜单栏中的菜单，随后将主窗口拖到 B 显示器；已打开菜单仍留在 A，菜单选项可以点击，点击后新打开的窗口/界面回到 B。
- 当前证据：维护者报告，尚未独立复现；单屏移动、旧版本对照、关闭后重新展开菜单等对照结果未取得。
- 关联判断：输入候选生产改动限于 `src/xorg-server/os/io.c` 的请求长度与缓冲区处理；候选和当前主线的 `winmultiwindowwndproc.c`、`winmultiwindowwindow.c`、`winmultiwindowwm.c` 文件哈希一致。暂未发现与输入修复的直接关联，但这不证明它一定是既有问题。
- 处理决定：维护者要求“先记录，以后再看”，暂缓排查；未修复，不将它描述为本次输入修复已引入或已排除的回归。
- 后续排查：确认异常 gitk 的运行端和 X Server 路径；对比未包含输入修复的主线，并检查同屏移动是否复现，再定位菜单生命周期与窗口位置更新。

已取得这份候选本身的实际使用反馈，并完成补丁恢复、目录适配、重新构建验证及主线整合；剪贴板、OpenGL 等未验证范围及 GUI-001 继续保留，不能据此宣布全场景通过。

该目录现为普通保留目录，不能再按 Git worktree 操作。候选源码另有 `.local-validation/reorganize-20260910/pending-input-source.patch` 和同目录 `current-development.bundle` 归档，恢复入口见 [仓库整理报告](../validation/2026-09-10-repository-reorganization.md)。验证详情见[输入候选报告](../validation/2026-09-09-xserver-input.md)。
