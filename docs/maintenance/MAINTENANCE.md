# 本地维护路线与关注事项

更新日期：2026-09-10。当前任务状态以 [计划状态](PLAN_STATUS.md) 为入口；本文件保留目标、验收层次和工作约定。

## 维护目标和边界

先维护一个自己能够长期使用、能够解释构建过程和定位故障的 Windows 版本。`origin` 为 `git@github.com:jackfahdin/ZzXsrv.git`，首次源码推送已完成。前次中文 README、脚本和文档整理的验证见[目录迁移报告](../validation/2026-09-10-readme-layout.md)；当前源码分层边界与旧路径对应见[源码目录设计](../designs/2026-09-10-source-layout.md)和[路径映射](../history/2026-09-10-source-path-mapping.json)。这些整理不包含产品发布、CI 配置或功能开发。

仓库仅保留线性 `master`，不再保留 `upstream`、旧工作分支或旧标签。23 个首父步骤已按 tree 无损重建，重建终点 `b8be16d` 的文件树与整理前 `a296b1553` 相同；后续提交记录本轮文档变更。11 个旧 worktree 已解除登记，源码和运行文件仍在原路径，后续应按普通目录使用。来源见 [docs/PROVENANCE.md](../PROVENANCE.md) 和 [依赖来源清单](../dependencies/SOURCES.md)，维护方法见 [上游更新规则](UPSTREAM.md)，映射与归档见 [仓库整理报告](../validation/2026-09-10-repository-reorganization.md)。旧报告中的提交号和标签名作为历史证据保留，不能当作当前 Git 引用使用。

首个支持目标是 Windows x64 Release。构建使用 PowerShell、VS2022/MSVC 和原生 Windows 辅助工具，不依赖 WSL/Cygwin，不自动下载或安装工具。Python 下限为 3.11。源码路径保持无空格；已有工具路径含空格的测试继续保留。

最初已验证基线的历史源码提交为 `a4adc3dc3f2158c2308133cecee19a71cf62bcc8`，当时标签名为 `local-baseline-20260909-a4adc3dc`。当时完成了独立检出的完整构建、43 项测试（无跳过）、35 个 PE 的依赖检查与经认证的本地 TCP X 根窗口查询。详情见 [实际验证报告](../validation/2026-09-09-baseline.md)。目前主线包含后续 GLX 和 XFIXES 修复，具体来源和运行目录见计划状态；旧标签已归档后删除。目录迁移后的构建验证单独记录，不增加多机或应用兼容性结论。

## 阶段安排

| 阶段 | 交付物 | 完成条件 | 当前状态 |
| --- | --- | --- | --- |
| 1：构建与运行基线 | 环境报告、运行验证工具、干净检出构建记录、历史标签记录 | 第一阶段规格中 B1–B6 全部满足 | 2026-09-09 已完成，见实际验证报告 |
| 2：日常兼容性 | 场景清单、复现材料、逐项结果 | 每个实际使用场景都有通过、失败或有原因的未验证状态 | 2026-09-09 用户反馈实际测试无问题；尚未逐项记录应用和场景 |
| 3：使用体验 | 启动配置、日志入口、错误提示、中文说明的独立改动 | 每项改动解决已记录的问题，有相应验证 | 按第二阶段问题的影响排序 |
| 4：工程演进 | 第三方组件清单、更新流程、分组件 CMake 试验 | 能说明变更范围，旧流程可作对照，运行结果无已知回归 | 来源目录盘点与单主线维护规则已整理，未知来源明确标注；GLX 与 XFIXES 均已整合；依赖升级与 CMake 试验未执行 |

第一阶段的执行入口见 [执行计划](../plans/2026-09-09-maintenance-baseline.md)，验收依据见 [验收规格](../designs/2026-09-09-maintenance-baseline-design.md)。原生构建的历史记录见 [原生构建计划](../plans/2026-09-08-native-windows-build.md)。

首轮 [组件更新评估](../validation/2026-09-09-component-update-assessment.md) 已记录实际消费者、候选版本、证据边界和验证门槛。首项 GLX 修复及构建中发现的问题见 [GLX 修复记录](../validation/2026-09-09-glx-context-tags.md)；2026-09-09 维护者对新版本反馈“能用”后，已本地快进整合到 master，合入后 51 项测试通过。具体应用与场景覆盖仍待补充，新旧运行目录和验证证据均保留。[XFIXES 实施计划](../plans/2026-09-09-xfixes-request-length.md) 也已完成：维护者确认使用正常，合入后 59 项测试通过。输入候选仍待实际使用确认；其余 X Server 与字体、XML、OpenSSL 维护保留为后续待办，本轮不执行。

## 验证层次

| 层次 | 验证内容 | 能证明什么 | 不能替代什么 |
| --- | --- | --- | --- |
| 脚本测试 | 路径、退出码、环境恢复、生成数据、运行目录整理 | 被测试的脚本行为符合预期 | 实际编译、GUI 使用 |
| 干净检出构建 | 新目录、固定提交、现有工具、All 阶段 | 不依赖旧工作目录中的未跟踪中间产物 | 全部第三方库均由源码构建、二进制逐字节一致 |
| 运行依赖检查 | PE 架构、导入 DLL、本地运行目录、系统目录 | 已扫描的直接/延迟导入满足规则 | 任意运行路径的动态加载 |
| 启动与连接 | 版本、启动、认证、X 根窗口查询、进程清理 | 最小 X 服务可使用 | OpenGL 正确性、输入法、剪贴板、多显示器 |
| 日常应用 | 实际 Linux 应用与 Windows 交互 | 指定版本、机器与场景下的行为 | 未测试的发行版、显卡和网络环境 |

测试结果使用 `PASS`、`FAIL`、`NOT_RUN`、`SKIP` 四种状态；跳过不等于通过。固定记录源码提交、工具版本、命令、退出码、日志路径和测试范围。机器完整日志放在本地忽略目录，提交经过检查的结果摘要。

## 持续关注的方向

| 方向 | 具体风险或问题 | 处理方式与触发条件 |
| --- | --- | --- |
| 工具链和 PATH | 开发机能运行，普通环境缺 DLL；误用另一个 Python/MSVC | 记录实际选中路径和版本；运行测试使用最小 PATH；工具升级后重跑第一阶段 |
| 第三方源码和预编译文件 | 仓库中同时存在源码和预编译依赖，例如 libxml2 | 建立来源、版本、补丁和构建方式清单；保留原版权与许可证材料；公开发布前核对随包材料 |
| 上游差异 | 本地修改与上游升级混在一起，难以合并和回退 | 在依赖来源清单记录固定来源、证据和未知项；组件更新按独立提交进入线性 master，保留适配差异与验证记录，不再维护来源分支 |
| 生成器接口 | XKB 的 Meson 数据格式或 Python 脚本参数变化 | 更新相关组件时运行真实数据测试；解析不支持的格式时明确失败 |
| 运行目录更新 | 文件清单改变后旧 DLL 留存；运行中的 EXE 无法覆盖 | 基线从新目录组装；增量整理单独测试；失败报告源、目标和原因，不默默忽略 |
| 网络认证 | 为让测试通过而关闭访问控制，或修改机器防火墙 | 使用临时认证文件；不使用 `-ac`，不更改全局防火墙；测试只管理自己启动的进程 |
| 图形、输入和字体 | 显卡、缩放、键盘布局、中文显示/输入行为不同 | 第二阶段分别记录环境与结果；中文显示和中文输入分开验收 |
| 发布和版本 | 上游版本与自维护版本混淆，无法定位用户问题 | 用固定源码提交和运行目录关联验证；历史标签查归档。产品发布前另行设计版本标识与发布说明 |
| CMake 迁移 | 同时替换全部构建规则导致故障难定位 | 完成基线后选一个边界清楚的组件试验；比较生成文件、链接依赖和运行结果 |

## 本地工作约定

- 文档、代码和验证结果在 Git 中保持关联；一次提交解决一个有意义的问题。
- 提交信息采用中文简短标题，空一行后用中文详细说明改动内容、原因和验证结果。
- 有行为变更时先建立针对原始问题的测试；纯文档变更检查链接、命令和覆盖关系即可。
- 执行第一阶段采用当前任务内顺序推进；独立审查可按适用技能安排，不要求维护者每一步重新授权。
- 超出当前授权范围的下载/安装、真实 Linux 环境接入或产品发布，说明具体需要再处理；本轮向已配置 origin 的首次推送已在仓库整理范围内。
- 干净构建使用明确固定来源的独立目录，保留主工作目录和已有运行目录。已解除登记的旧目录不能再按 worktree 操作，也不能对其运行 `git -C` 后假定正在访问旧检出；不使用全仓库 `git clean -xfd`。
- 本地 Git 历史不等于备份。旧引用与待验收输入候选已另存本地归档；普通首次推送不会上传忽略目录中的归档、日志或运行文件。

## 第二阶段场景入口

2026-09-09 维护者反馈“我刚刚验证了，没问题”，记录为实际测试功能通过，未报告故障。同日只读核对原作者 master，与已导入提交完全相同，当时没有新的原作者提交需要同步。这是当时的历史来源检查，不再作为当前同步任务。详情见 [用户验证与上游检查](../validation/2026-09-09-user-validation-and-upstream-check.md)。

逐项记录入口为 [兼容性记录](COMPATIBILITY.md)。先记录 Linux 发行版/版本、应用/版本、连接方式、认证方式、Windows 版本、显卡与驱动、显示器及缩放。Linux 可以在远程机器或虚拟机中运行；这不改变 Windows 构建不依赖 WSL 的要求。

场景至少包括：启动/关闭/重启、常用 GUI、文本剪贴板两个方向、中文显示、中文输入、多窗口、多显示器与缩放、一个实际需要的 OpenGL 应用。每项记录操作步骤、期望行为、实际行为、日志和复现频率。当前这些场景均不自动标记为通过。
