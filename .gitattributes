# Preserve the synchronized upstream iconv header, including upstream whitespace.
include/iconv.h -text -whitespace
# This inherited package inventory is stored with CRLF in Git.
docs/dependencies/packages.txt whitespace=cr-at-eol
# Preserve the complete official OpenSSL release, including upstream whitespace.
third_party/openssl/** -text -whitespace
# Preserve release archive bytes and explicitly recorded local adaptations.
third_party/xorg/xkbcomp/** -text -whitespace
third_party/xorg/libXpm/** -text -whitespace
third_party/zlib/** -text -whitespace
include/X11/xpm.h -text -whitespace
third_party/xorg/libX11/** -text -whitespace
third_party/xorg/libxcb/patches/** -text -whitespace
